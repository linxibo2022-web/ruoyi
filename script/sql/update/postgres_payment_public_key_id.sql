-- ============================================================================
-- b_payment 新增「微信支付公钥ID」字段 增量升级脚本 (PostgreSQL)
-- ============================================================================
-- 变更内容：
--   b_payment 新增 public_key_id 字段（微信支付 V3 公钥模式专用）
-- 触发原因：
--   微信支付 V3「公钥模式」验签需要独立的公钥ID(PUB_KEY_ID_xxx)，
--   原实现误用证书序列号(cert_serial_no)冒充，导致公钥模式验签失败。
-- 引入版本：2026-05 同步批次
-- 升级前请备份 b_payment 表！
-- ============================================================================

ALTER TABLE b_payment ADD COLUMN public_key_id VARCHAR(100) DEFAULT NULL::VARCHAR;
COMMENT ON COLUMN b_payment.public_key_id IS '微信支付公钥ID(V3公钥模式专用,格式 PUB_KEY_ID_xxx)';

-- ============================================================================
-- 回滚 SQL（如升级后需回退）
-- ============================================================================
-- ALTER TABLE b_payment DROP COLUMN public_key_id;
