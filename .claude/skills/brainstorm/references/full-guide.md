
# 头脑风暴框架

## 本项目技术约束（头脑风暴边界）

> **重要**：所有方案必须在以下技术栈约束内思考

### 后端技术边界

| 层面 | 已有能力 | 不在范围内 |
|------|---------|-----------|
| **框架** | Spring Boot 3.5.8 | Spring Cloud 微服务 |
| **ORM** | MyBatis-Plus 3.5.14 | JPA/Hibernate |
| **缓存** | Redis + Redisson | Memcached |
| **消息** | RocketMQ 5.3.1、Redis Streams | Kafka、RabbitMQ |
| **定时** | @Scheduled、SnailJob | XXL-JOB、Quartz |
| **认证** | Sa-Token 1.44.0 | Spring Security |
| **AI** | LangChain4j 0.35.0 | 其他 AI 框架 |

### 前端技术边界

| 端 | 框架 | UI 组件 | 状态管理 |
|----|------|---------|---------|
| **PC** | Vue 3.5.13 | Element Plus (A* 封装) | Pinia |
| **移动端** | UniApp + Vue 3.4.21 | WD UI (wd-* 组件) | Pinia |

### 可扩展的 35 个 Common 模块

```
核心: core, web, security, log
数据: mybatis, redis, json, encrypt, excel, sensitive, oss, serialmap
认证: satoken, tenant, social
通信: mail, sms, websocket, sse, message, http
业务: job, rocketmq, mqtt, idempotent, ratelimiter
微信: miniapp, mp, pay, pay-core, pay-wechat, pay-alipay, pay-balance, pay-unionpay
其他: doc, openapi, langchain4j, media, test
```

---

## 头脑风暴交互纪律（分段确认 · 必须遵守）

> **纪律：不要一次性吐出完整方案**。按段提问、逐段确认，让用户在每一步把关，避免"方向跑偏到最后才发现"。

### 分段推进（每段确认后再进下一段）

| 段 | 你做什么 | 闸门 |
|----|---------|------|
| ① 澄清问题 | 用 1-3 个关键问题问清"要解决什么、给谁用、边界在哪"（参考"讨论引导问题"），**一次别问太多** | 用户答完才进 ② |
| ② 资源盘点 | 列可复用的 common 模块 / 现有功能（参考"可复用资源盘点"），指出哪些要新开发 | 用户认可才进 ③ |
| ③ 方案对比 | 给 2-3 个方案 + 评分矩阵（"方案评估矩阵"），**逐个讲、不要堆** | 用户选定方向才进 ④ |
| ④ 推荐定稿 | 按选定方向给推荐方案 + 实施步骤 + 风险 | 用户点头才保存结论 |

### 纪律要点
- **一段一确认**：每段结束明确问"这段 OK 吗 / 选哪个 / 要改吗"，得到回应再继续。
- **不臆造**：信息不足先问，别替用户假设（本框架是商业产品，事实以项目档案 + 用户原话为准）。
- **可降级**：用户说"别问了，直接给方案"时，可一次性输出，但仍保留分段结构便于回看。

---

## 思维模式

### 发散思维 - 尽可能多的方案

1. **不评判**: 先不考虑可行性，尽可能多地列出想法
2. **联想**: 从一个想法延伸到相关想法
3. **模块复用**: 能否用现有 35 个 common 模块解决？
4. **跨界借鉴**: 其他系统怎么做的？
5. **反向思考**: 如果要让问题更糟，会怎么做？反过来就是解决方案

### 收敛思维 - 筛选最优方案

1. **复用优先**: 能否复用现有模块/代码？
2. **可行性**: 在现有技术栈内能否实现？
3. **多端一致**: 是否需要 PC 端和移动端同时支持？
4. **多租户兼容**: 是否支持多租户数据隔离？
5. **多数据库**: 是否需要兼容 MySQL/Oracle/PostgreSQL/SQL Server？
6. **成本**: 开发时间和复杂度如何？
7. **风险**: 有什么潜在问题？

---

## 本项目专属决策维度

### 必须考虑的 5 个维度

| 维度 | 问题 | 影响范围 |
|------|------|---------|
| **多租户** | 数据是否需要租户隔离？ | Entity 继承 TenantEntity、SQL 自动过滤 |
| **多端** | PC 端、移动端、小程序都需要吗？ | API 设计、权限控制 |
| **多数据库** | 需要兼容哪些数据库？ | SQL 语法、`likeCast()` |
| **模块归属** | 属于 base/mall/crm/iot 哪个模块？ | 表前缀、包路径 |
| **权限粒度** | 需要什么级别的权限控制？ | 菜单权限、数据权限 |

### 方案评估矩阵（本项目版）

```markdown
| 方案 | 复用度(25%) | 多端(20%) | 多租户(20%) | 多数据库(15%) | 开发量(20%) | 总分 |
|------|-------------|-----------|-------------|---------------|-------------|------|
| 方案A | 9 | 8 | 10 | 10 | 7 | 8.65 |
| 方案B | 6 | 10 | 10 | 8 | 8 | 8.30 |
| 方案C | 10 | 6 | 8 | 6 | 9 | 8.00 |

评分说明：1-10分，分数越高越好
- 复用度：能复用多少现有代码/模块
- 多端：PC端和移动端支持程度
- 多租户：租户隔离支持程度
- 多数据库：跨数据库兼容性
- 开发量：开发工作量（分数越高=工作量越少）
```

---

## 方案探索模板

### 1. 问题定义（本项目版）

```markdown
## 问题描述

### 基本信息
- **是什么**: [具体描述要实现的功能]
- **为什么重要**: [业务价值]
- **当前状态**: [现有系统如何处理/有无类似功能]
- **期望结果**: [希望达到的效果]

### 本项目约束
- **模块归属**: base / mall / crm / iot / 新模块
- **端支持**: PC端 / 移动端 / 两者都需要
- **租户**: 需要租户隔离 / 不需要（系统级）
- **数据库**: 只支持 MySQL / 需要多数据库兼容
```

### 2. 可复用资源盘点

```markdown
## 现有资源

### 可复用的 Common 模块
| 模块 | 是否可用 | 用途 |
|------|---------|------|
| ruoyi-common-redis | ✅ | 缓存、分布式锁 |
| ruoyi-common-websocket | ❓ | 实时推送 |
| ruoyi-common-pay | ❓ | 支付功能 |
| ... | | |

### 可参考的现有功能
| 功能 | 位置 | 可复用部分 |
|------|------|-----------|
| 广告管理 | base/Ad | 完整 CRUD 模板 |
| 平台配置 | base/Platform | 配置管理模式 |
| 订阅消息 | api/app/Subscribe | 微信消息模板 |
| ... | | |

### 需要新开发的部分
- [ ] [需要开发的内容]
```

### 3. 方案列举

```markdown
## 可能方案

### 方案 A: 最大复用（推荐优先考虑）
- **描述**: 基于现有模块组合实现
- **复用**: [列出复用的模块/代码]
- **新开发**: [列出必须新写的部分]
- **优点**: 开发快、稳定性高
- **缺点**: [可能的限制]

### 方案 B: 适度扩展
- **描述**: 在现有基础上扩展新功能
- **复用**: [列出复用的模块/代码]
- **新开发**: [列出必须新写的部分]
- **优点**: 平衡复用与定制
- **缺点**: [可能的限制]

### 方案 C: 全新开发
- **描述**: 从零开始实现
- **复用**: 仅复用基础框架
- **新开发**: 全部业务逻辑
- **优点**: 完全定制、无历史包袱
- **缺点**: 开发量大、风险高
```

### 4. 推荐方案

```markdown
## 推荐方案

**推荐**: 方案 [X]

**理由**:
1. 复用了 [xxx] 模块，减少 [xx%] 开发量
2. 完全支持多租户/多端/多数据库
3. [其他理由]

**实施步骤**:
1. [ ] 数据库设计 → 使用 `/crud` 或 `database-ops` Skill
2. [ ] 后端开发 → 使用 `crud-development` Skill
3. [ ] PC 前端 → 使用 `ui-pc` Skill
4. [ ] 移动端 → 使用 `ui-mobile` Skill
5. [ ] 测试验证

**风险点**:
- [风险1] → 应对策略
- [风险2] → 应对策略
```

---

## 本项目典型业务场景

### 场景 1：新增业务管理模块

```
需求：新增"优惠券管理"功能

头脑风暴：
├── 模块归属
│   ├── mall（商城相关）✅ 推荐
│   ├── base（基础业务）
│   └── 新建 coupon 模块
│
├── 功能拆解
│   ├── 优惠券模板管理（后台配置）
│   ├── 优惠券发放（手动/自动/领取）
│   ├── 优惠券使用（订单抵扣）
│   └── 优惠券统计（使用率/核销率）
│
├── 可复用
│   ├── Ad 模块 → CRUD 模板
│   ├── ruoyi-common-redis → 库存扣减锁
│   ├── ruoyi-common-job → 过期自动作废
│   └── 字典管理 → 券类型/状态字典
│
└── 技术方案
    ├── 表设计：m_coupon_template, m_coupon_record
    ├── 后端：标准四层架构
    ├── PC 端：管理页面
    └── 移动端：领券中心页面
```

### 场景 2：接入第三方服务

```
需求：接入物流查询功能

头脑风暴：
├── 方案选择
│   ├── 方案 A：使用快递100 API
│   ├── 方案 B：使用快递鸟 API
│   └── 方案 C：自建物流爬虫（不推荐）
│
├── 可复用
│   ├── ruoyi-common-http → HTTP 客户端
│   ├── ruoyi-common-redis → 结果缓存
│   ├── ruoyi-common-encrypt → 密钥加密存储
│   └── Platform 模块 → 配置存储模式
│
├── 实现方式
│   ├── 新建 LogisticsService 接口
│   ├── 实现 Kuaidi100LogisticsImpl
│   ├── 支持后续扩展其他供应商
│   └── 配置项存入 sys_config 表
│
└── 注意事项
    ├── API 密钥加密存储
    ├── 调用频率限制
    ├── 查询结果缓存（5分钟）
    └── 失败降级处理
```

### 场景 3：实时通信需求

```
需求：实现消息实时推送

头脑风暴：
├── 技术选型
│   ├── WebSocket（ruoyi-common-websocket）✅ 首选
│   │   └── 适用：聊天、通知、状态同步
│   ├── SSE（ruoyi-common-sse）
│   │   └── 适用：服务端单向推送
│   └── 轮询（不推荐）
│
├── 消息类型
│   ├── 系统通知 → 广播
│   ├── 个人消息 → 点对点
│   ├── 群组消息 → 组播
│   └── 订单状态 → 主题订阅
│
├── 可复用
│   ├── ruoyi-common-websocket → 完整方案
│   ├── ruoyi-common-redis → 在线状态/消息队列
│   └── 现有 WebSocket 示例
│
└── 移动端适配
    ├── 小程序：使用 SocketTask
    ├── H5：原生 WebSocket
    └── APP：uni-socket
```

### 场景 4：支付功能扩展

```
需求：增加支付方式

头脑风暴：
├── 已支持
│   ├── 微信支付（ruoyi-common-pay-wechat）✅
│   ├── 支付宝（ruoyi-common-pay-alipay）✅
│   ├── 余额支付（ruoyi-common-pay-balance）✅
│   └── 银联支付（ruoyi-common-pay-unionpay）✅
│
├── 扩展方向
│   ├── 组合支付（余额+微信）
│   ├── 分账功能
│   ├── 退款流程优化
│   └── 支付对账
│
├── 可复用
│   ├── ruoyi-common-pay-core → 支付抽象
│   ├── Payment 模块 → 支付配置管理
│   ├── ruoyi-common-job → 定时对账
│   └── ruoyi-common-redis → 支付幂等
│
└── 注意事项
    ├── 多租户支付配置隔离
    ├── 支付安全（签名验证）
    ├── 回调可靠性（重试机制）
    └── 资金安全（事务一致性）
```

### 场景 5：数据统计需求

```
需求：业务数据统计报表

头脑风暴：
├── 统计维度
│   ├── 时间：日/周/月/年
│   ├── 租户：单租户/全平台
│   ├── 业务：订单/用户/商品
│   └── 对比：同比/环比
│
├── 实现方案
│   ├── 方案 A：实时查询（小数据量）
│   ├── 方案 B：预计算 + 定时任务（中数据量）✅
│   └── 方案 C：数据仓库（大数据量）
│
├── 可复用
│   ├── StatisticsController → 统计 API 模板
│   ├── ruoyi-common-excel → 导出功能
│   ├── ruoyi-common-job → 定时统计
│   ├── ruoyi-common-redis → 结果缓存
│   └── PC 端图表组件（AChart*）
│
└── 数据库优化
    ├── 统计表设计（星型/雪花模型）
    ├── 索引优化
    ├── 分区表（按时间）
    └── 只读从库
```

### 场景 6：AI 功能集成

```
需求：增加 AI 辅助功能

头脑风暴：
├── 已有能力
│   ├── ruoyi-common-langchain4j → AI 集成框架
│   ├── AiChatController → 对话示例
│   └── PC 端 AI 组件（AAi*）
│
├── 扩展方向
│   ├── 智能客服
│   ├── 内容生成
│   ├── 数据分析
│   └── 图片处理
│
├── 模型选择
│   ├── ChatGPT（OpenAI）
│   ├── DeepSeek（国产）
│   ├── 通义千问
│   └── 文心一言
│
└── 实现要点
    ├── 流式响应（SSE）
    ├── 上下文管理
    ├── Token 计费
    └── 敏感词过滤
```

---

## 创意激发技巧（本项目版）

### 1. 模块组合法

```
问题：如何实现活动抽奖功能？

模块组合：
├── ruoyi-common-redis → 库存/次数控制
├── ruoyi-common-ratelimiter → 防刷
├── ruoyi-common-idempotent → 防重复
├── ruoyi-common-websocket → 实时开奖
├── ruoyi-common-job → 定时开奖
└── 现有订单流程 → 奖品发放

组合方案：
1. 活动配置（参考 Ad 模块）
2. 抽奖服务（Redis Lua 脚本原子操作）
3. 中奖记录（标准 CRUD）
4. 奖品发放（复用订单流程）
```

### 2. 现有功能类比法

```
问题：如何实现预约功能？

类比分析：
├── 类似订单 → 创建、状态流转、取消
├── 类似活动 → 时间段、名额限制
└── 类似秒杀 → 库存控制、并发处理

复用点：
├── 订单状态机 → 预约状态流转
├── 库存扣减逻辑 → 名额控制
├── 支付流程 → 预约押金
└── 消息通知 → 预约提醒
```

### 3. 端优先思考法

```
问题：功能应该在哪个端实现？

决策树：
├── 管理操作（配置、审核）→ PC 端
├── 用户操作（下单、查看）→ 移动端
├── 实时展示（监控、大屏）→ PC 端
└── 高频操作（签到、扫码）→ 移动端

多端同步考虑：
├── 数据实时性 → WebSocket
├── 操作幂等性 → Redis 分布式锁
└── 状态一致性 → 事件驱动
```

### 4. 多租户思考法

```
问题：功能是否需要租户隔离？

判断标准：
├── 租户私有数据 → 必须隔离
│   └── 用户、订单、商品、配置
├── 平台公共数据 → 不需要隔离
│   └── 字典、地区、系统配置
└── 混合数据 → 条件隔离
    └── 模板（平台模板 + 租户模板）

实现方式：
├── Entity 继承 TenantEntity
├── 自动 SQL 过滤
└── 租户配置表设计
```

### 5. 联网借鉴法（按需触发，不是默认动作）

何时**该**联网查：
- 涉及新技术 / 新版本，训练数据可能过时
- 用户明确说"业界怎么做"、"看看竞品"、"参考官方文档"
- 项目现有 35 个 common 模块覆盖不到，必须引入外部方案
- 准备给出技术选型，但你对该领域的最新最佳实践没把握

何时**不该**联网：
- 项目内功能（CRUD / UI / 常见 common 模块组合）
- 已经有明确方向，只是要细化
- 一两秒就能想清楚的判断（如"是否需要租户隔离"）
- 用户只想要一个快速发散，没要求深度调研

> **工具选择**参考 CLAUDE.md 顶部「MCP 工具触发」与全局指令的「WebFetch 抓取策略」：
> - 主流库官方文档 → `context7` MCP
> - 业界案例 / 竞品 → WebSearch（失败时 URL 前加 `https://r.jina.ai/` 重试）
> - 特定页面深度抓取 → `chrome-devtools` MCP

---

## 讨论引导问题（本项目版）

### 功能规划时

1. **复用视角**
   - 有没有类似的现有功能？
   - 能复用哪些 common 模块？
   - 需要新开发多少代码？

2. **多端视角**
   - PC 端需要什么？
   - 移动端需要什么？
   - 两端数据如何同步？

3. **多租户视角**
   - 数据需要租户隔离吗？
   - 配置是租户级还是系统级？
   - 不同租户能定制吗？

4. **技术视角**
   - 现有技术栈能实现吗？
   - 需要引入新技术吗？
   - 有没有性能瓶颈？

### 方案选择时

1. **短期 vs 长期**
   - 快速上线重要还是可扩展重要？
   - 以后可能怎么扩展？

2. **简单 vs 完整**
   - MVP 版本包含什么？
   - 完整版本还需要什么？

3. **风险评估**
   - 最大的技术风险是什么？
   - 最大的业务风险是什么？
   - 如何降低风险？

---

## 与其他 Skill 联动

### 头脑风暴 → 实施的路径

```
brainstorm（本 Skill）
    ↓ 确定方案后
    │
    ├── 需要建表 → database-ops
    │
    ├── 需要后端开发 → crud-development
    │   └── 需要高级注解 → backend-annotations
    │
    ├── 需要 PC 前端 → ui-pc
    │   └── 需要状态管理 → store-pc
    │
    ├── 需要移动端 → ui-mobile
    │   └── 需要状态管理 → store-mobile
    │
    ├── 需要支付 → payment-integration
    │
    ├── 需要微信功能 → wechat-integration
    │
    ├── 需要 AI → ai-langchain4j
    │
    ├── 需要技术选型 → tech-decision
    │
    ├──（UI 类）出 HTML 原型 → 工作站 ui-studio → docs/prototypes/
    │
    ├── 拆可执行计划 → writing-plans → docs/tasks/active/ → /dev-loop 执行
    │
    └── 或轻量落地 → /init-docs {功能描述}
```

### 快速触发其他 Skill

| 头脑风暴结论 | 下一步 Skill | 触发方式 |
|-------------|-------------|---------|
| 需要新建业务模块 | crud-development | "帮我开发 xxx 模块" |
| 需要数据库设计 | database-ops | "帮我设计 xxx 表" |
| 需要 PC 页面 | ui-pc | "帮我做 xxx 管理页面" |
| 需要移动端页面 | ui-mobile | "帮我做 xxx 小程序页面" |
| 需要接入支付 | payment-integration | "帮我接入 xxx 支付" |
| 需要技术对比 | tech-decision | "xxx 和 xxx 哪个好" |
| 方案确定，要初始化项目文档 | /init-docs | `/init-docs 实现xxx功能` |

---

## 头脑风暴输出模板

### 完整输出格式

```markdown
# [功能名称] 方案设计

## 1. 问题定义
- 需求描述：[xxx]
- 业务价值：[xxx]
- 模块归属：[base/mall/crm/iot]
- 端支持：[PC/移动端/两者]

## 2. 可复用资源
| 资源 | 类型 | 复用程度 |
|------|------|---------|
| ruoyi-common-xxx | 模块 | 完全复用 |
| Xxx 功能 | 代码 | 参考模式 |

## 3. 方案对比
| 维度 | 方案 A | 方案 B |
|------|-------|-------|
| 复用度 | 高 | 中 |
| 开发量 | 小 | 中 |
| 扩展性 | 中 | 高 |

## 4. 推荐方案
**方案 A** - [理由]

## 5. 实施步骤
1. [ ] xxx
2. [ ] xxx
3. [ ] xxx

## 6. 风险与应对
| 风险 | 应对策略 |
|------|---------|
| xxx | xxx |
```

### 头脑风暴结论保存（必须执行）

头脑风暴完成后，**必须**将结论保存为文档，方便后续跨会话查看和更新。

**保存路径**：`docs/brainstorm-{主题简称}.md`（主题简称用中文，如 `brainstorm-用户反馈.md`）

**保存内容**：将本次头脑风暴的完整输出（方案对比、分析过程、最终结论）写入文件。

**文档格式**：

```markdown
# 头脑风暴：{主题名称}

**创建时间**: YYYY-MM-DD HH:MM
**最后更新**: YYYY-MM-DD HH:MM
**状态**: 讨论中 / 已确定方案 / 已开始开发

---

## 背景与目标

{为什么要做这件事，要解决什么问题}

## 方案讨论

### 方案 A：{名称}

- **描述**: {一句话概述}
- **优点**: {列表}
- **缺点**: {列表}

### 方案 B：{名称}

- **描述**: {一句话概述}
- **优点**: {列表}
- **缺点**: {列表}

## 最终结论

**选定方案**: {方案名称}

**选择理由**:
1. {理由}
2. {理由}

**实施步骤**:
1. {步骤}
2. {步骤}

## 风险与注意事项

| 风险 | 应对策略 |
|------|---------|
| {风险} | {策略} |

## 讨论记录

### YYYY-MM-DD 初始讨论
- {本次讨论的要点}

{后续更新在此追加新的讨论记录}
```

**更新规则**（后续会话中继续讨论时）：
1. 读取已有的 `docs/brainstorm-{主题}.md`
2. 在「讨论记录」章节追加新的日期和讨论内容
3. 如果结论有变化，更新「最终结论」章节
4. 更新「最后更新」时间和「状态」

**保存后向用户确认**：

```
📄 已保存头脑风暴结论到: docs/brainstorm-{主题}.md
后续可随时查看和更新此文档。
```

## HTML 原型生成（UI 类需求 · 优先工作站，禁手搓）

方案确定后，**若涉及页面 UI**（后台管理页 / 移动端页 / 大屏等），优先用可视化原型代替纯文字描述，让用户"看着确认"。

### 必须遵守
- 🔴 **禁止手搓 HTML 充原型**（CLAUDE.md 硬规）。一律走 **AI 工作站 ui-studio**：
  `ai-workstation MCP → route(query=按需求做 PC后台/移动端 UI 原型) → get_skill(ai-ui-studio)` → 产出到 `docs/prototypes/<业务>/*.html`。
- 原型 HTML 带"组件映射表"注释（PC 对应 plus-ui `A*` 封装组件、移动端对应 plus-uniapp `wd-*`），便于后续 `html-to-code` 转码 + `dev-loop` 截图逐元素对照。
- **纯后端 / 无界面需求 → 跳过本步**，别画蛇添足。

### 与下游衔接
- 原型 → `writing-plans` 把 UI 任务写成"`html-to-code` 转码 + e2e 截图对照原型"。
- 原型缺失但有 UI 任务时，`writing-plans`/`dev-loop` 会回标"先补原型"，**不自行手搓**。

---

### 保存后的下一步建议

保存结论后，向用户展示（**新链路：方案 →（原型）→ 计划 → 执行**）：

```markdown
💡 **下一步建议**：

1. **（UI 类）先出原型** → 工作站 ui-studio 产 `docs/prototypes/<业务>/*.html`
2. **拆可执行计划** → 激活 `writing-plans`，把方案落成 `docs/tasks/active/` 细颗粒计划
3. **自主执行** → `/loop /dev-loop`
4. （轻量）**初始化项目文档** → `/init-docs {功能描述}`；**直接开发** → `/dev`
```

---

## 常见问题快速决策

| 问题 | 推荐方案 | 原因 |
|------|---------|------|
| 新功能放哪个模块？ | 看业务相关性 | base=基础、mall=商城、crm=客户、iot=设备 |
| 需要新建 common 模块吗？ | 通常不需要 | 35 个模块覆盖大部分场景 |
| 用 WebSocket 还是 SSE？ | WebSocket | 双向通信、已有封装 |
| 用 RocketMQ 还是 Redis？ | Redis 优先 | 简单场景够用，复杂再用 MQ |
| 要不要支持多数据库？ | 是 | 项目标准要求 |
| 要不要支持多租户？ | 业务数据要 | 继承 TenantEntity |
| PC 和移动端用同一个 API？ | 是 | 统一后端，按需扩展 |

---

## 🔗 关联技能边界

本技能专注于**开放式方案探索**（还不知道怎么做时）。遇到以下场景请改用其他技能：

| 场景 | 应使用技能 | 判断关键词 |
|------|-----------|-----------|
| 方向已定、要设计模块分层 | `architecture-design` | "四层架构"、"模块划分" |
| 方向已定、要选 A 还是 B | `tech-decision` | "Redis vs Caffeine"、"哪个好" |
| 方案已定、要拆可执行计划 | `writing-plans` | "写计划"、"拆任务"、"实施步骤" |
| 已确定功能、要跟踪实施 | `task-tracker` | "创建任务跟踪"、"记录进度" |

**三阶段辨别**：
1. **brainstorm** = 方案发散阶段（输出 3-5 个方向）
2. **architecture-design** = 架构落地阶段（选定方向后分层）
3. **tech-decision** = 决策收敛阶段（两个方案二选一）

**输出链路**：`brainstorm` 确定方案 →（UI 类经工作站出 `docs/prototypes/*.html`）→ `writing-plans` 拆计划入 `docs/tasks/active/` → `/dev-loop` 执行（里程碑 `/update-status` 聚合）。
