# 后端开发指南 - ruoyi-plus-uniapp 项目

> ⚠️ **重要提醒**: 本项目是 **ruoyi-plus-uniapp**,不是 ruoyi-vue-plus!
> 架构设计完全不同,请严格遵循本项目的规范!

---

## 🚫 首要禁令

**绝对禁止参考 ruoyi-vue-plus 框架的代码风格!**

本项目的核心特征:
- ✅ 包名: `plus.ruoyi.*`
- ✅ Service **不继承任何基类**
- ✅ 独立的 **DAO层** 负责构建查询
- ✅ Entity 继承 `TenantEntity`
- ✅ BO 使用 `@AutoMappers` 自动映射

---

## 项目结构

```
ruoyi-plus-uniapp/
├── ruoyi-admin/                # 系统入口模块（主启动）
├── ruoyi-common/               # 通用工具模块（20+个子模块）
│   ├── ruoyi-common-core/      # 核心工具模块
│   ├── ruoyi-common-mybatis/   # MyBatis-Plus增强 (含DAO层基类)
│   ├── ruoyi-common-tenant/    # 多租户模块 (含TenantEntity)
│   ├── ruoyi-common-redis/     # Redis缓存模块
│   ├── ruoyi-common-satoken/   # 权限认证模块
│   └── ...                     # 其他20+个工具模块
├── ruoyi-modules/              # 业务功能模块
│   ├── ruoyi-system/           # 系统管理模块
│   ├── ruoyi-generator/        # 代码生成器模块
│   └── ruoyi-business/         # 自定义业务模块
│       └── src/main/java/plus/ruoyi/business/
│           ├── controller/     # PC端控制器
│           ├── api/            # API接口层
│           │   ├── app/        # 移动端API
│           │   └── pc/         # PC端API
│           ├── base/           # 基础业务服务模块
│           │   ├── controller/ # 基础业务控制器
│           │   ├── service/    # 业务服务层 (不继承基类)
│           │   │   └── impl/   # 服务实现
│           │   ├── dao/        # ⭐ 数据访问层 (本项目独有)
│           │   │   └── impl/   # DAO实现 (含buildQueryWrapper)
│           │   ├── mapper/     # MyBatis映射器
│           │   └── domain/     # 领域模型
│           │       ├── Entity.java  # 实体类 (继承TenantEntity)
│           │       ├── bo/     # 业务对象 (使用@AutoMappers)
│           │       └── vo/     # 视图对象
│           ├── job/            # 任务调度业务模块
│           └── mall/           # 商城业务模块
└── script/                     # 部署脚本和SQL
    └── sql/                    # 数据库初始化脚本
        ├── ry_plus_sys.sql     # 系统核心表
        ├── ry_plus_app.sql     # 移动端业务表
        ├── ry_plus_job.sql     # 任务调度表
        └── ry_plus_new.sql     # 新业务表
```

---

## 四层架构设计 (本项目独有)

### 1. Controller 层

**职责**: 接收请求、参数校验、返回响应

```java
package plus.ruoyi.business.base.controller;

import lombok.RequiredArgsConstructor;
import jakarta.validation.constraints.*;
import cn.dev33.satoken.annotation.SaCheckPermission;
import plus.ruoyi.common.core.constant.I18nKeys;
import org.springframework.web.bind.annotation.*;
import org.springframework.validation.annotation.Validated;
import plus.ruoyi.common.idempotent.annotation.RepeatSubmit;
import plus.ruoyi.common.log.annotation.Log;
import plus.ruoyi.common.mybatis.core.page.PageQuery;
import plus.ruoyi.common.mybatis.core.page.PageResult;
import plus.ruoyi.common.core.domain.R;
import plus.ruoyi.common.core.validate.AddGroup;
import plus.ruoyi.common.core.validate.EditGroup;
import plus.ruoyi.common.core.dict.DictOperType;
import plus.ruoyi.business.base.domain.vo.AdVo;
import plus.ruoyi.business.base.domain.bo.AdBo;
import plus.ruoyi.business.base.service.IAdService;

@Validated
@RequiredArgsConstructor
@RestController
@RequestMapping("/base/ad")
public class AdController {

    private final IAdService adService;  // ✅ 只注入Service

    @SaCheckPermission("base:ad:query")
    @GetMapping("/pageAds")
    public R<PageResult<AdVo>> pageAds(AdBo bo, PageQuery pageQuery) {
        return R.ok(adService.page(bo, pageQuery));
    }

    @SaCheckPermission("base:ad:query")
    @GetMapping("/getAd/{id}")
    public R<AdVo> getAd(@NotNull(message = I18nKeys.Common.ID_REQUIRED) @PathVariable Long id) {
        return R.ok(adService.get(id));
    }

    @SaCheckPermission("base:ad:add")
    @Log(title = "广告配置", operType = DictOperType.INSERT)
    @RepeatSubmit()
    @PostMapping("/addAd")
    public R<Long> addAd(@Validated(AddGroup.class) @RequestBody AdBo bo) {
        return R.ok(adService.add(bo));
    }

    @SaCheckPermission("base:ad:update")
    @Log(title = "广告配置", operType = DictOperType.UPDATE)
    @RepeatSubmit()
    @PutMapping("/updateAd")
    public R<Void> updateAd(@Validated(EditGroup.class) @RequestBody AdBo bo) {
        return R.status(adService.update(bo));
    }

    @SaCheckPermission("base:ad:delete")
    @Log(title = "广告配置", operType = DictOperType.DELETE)
    @DeleteMapping("/deleteAds/{ids}")
    public R<Void> deleteAds(@NotEmpty(message = I18nKeys.Common.ID_REQUIRED) @PathVariable Long[] ids) {
        return R.status(adService.batchDelete(List.of(ids)));
    }
}
```

**关键点**:
- ✅ 只注入 Service,不注入 DAO
- ✅ 使用 `@SaCheckPermission` 权限控制
- ✅ 使用 `@Log` 记录操作日志
- ✅ 使用 `@RepeatSubmit` 防止重复提交
- ✅ 返回值统一使用 `R<T>` 包装

### 2. Service 层

**职责**: 业务逻辑处理、对象转换、事务控制

#### Service 接口

```java
package plus.ruoyi.business.base.service;

import plus.ruoyi.business.base.domain.bo.AdBo;
import plus.ruoyi.business.base.domain.vo.AdVo;
import plus.ruoyi.common.mybatis.core.page.PageQuery;
import plus.ruoyi.common.mybatis.core.page.PageResult;
import java.util.Collection;
import java.util.List;

public interface IAdService {  // ✅ 不继承IBaseService

    AdVo get(Long id);
    List<AdVo> list(AdBo bo);
    PageResult<AdVo> page(AdBo bo, PageQuery pageQuery);
    Long add(AdBo bo);
    boolean update(AdBo bo);
    boolean batchDelete(Collection<Long> ids);
    boolean batchSave(List<AdBo> boList);
}
```

#### Service 实现类

```java
package plus.ruoyi.business.base.service.impl;

import cn.hutool.core.collection.CollUtil;
import lombok.RequiredArgsConstructor;
import org.springframework.stereotype.Service;
import org.springframework.transaction.annotation.Transactional;
import plus.ruoyi.business.base.dao.IAdDao;
import plus.ruoyi.business.base.domain.Ad;
import plus.ruoyi.business.base.domain.bo.AdBo;
import plus.ruoyi.business.base.domain.vo.AdVo;
import plus.ruoyi.business.base.service.IAdService;
import plus.ruoyi.common.core.exception.ServiceException;
import plus.ruoyi.common.core.utils.MapstructUtils;
import plus.ruoyi.common.mybatis.core.page.PageQuery;
import plus.ruoyi.common.mybatis.core.page.PageResult;
import plus.ruoyi.common.mybatis.core.query.PlusLambdaQuery;
import java.util.Collection;
import java.util.List;

@Service
@RequiredArgsConstructor
public class AdServiceImpl implements IAdService {  // ✅ 不继承任何基类

    private final IAdDao adDao;  // ✅ 只注入DAO

    @Override
    public AdVo get(Long id) {
        Ad entity = adDao.getById(id);
        return MapstructUtils.convert(entity, AdVo.class);
    }

    @Override
    public PageResult<AdVo> page(AdBo bo, PageQuery pageQuery) {
        // ✅ 调用DAO层的buildQueryWrapper方法获取查询条件
        PlusLambdaQuery<Ad> wrapper = adDao.buildQueryWrapper(bo);
        PageResult<Ad> entityPage = adDao.page(wrapper, pageQuery);
        // ✅ 使用PageResult自带的convert方法转换
        return entityPage.convert(AdVo.class);
    }

    @Override
    @Transactional(rollbackFor = Exception.class)
    public Long add(AdBo bo) {
        Ad entity = MapstructUtils.convert(bo, Ad.class);
        beforeSave(entity);  // 保存前钩子
        adDao.insert(entity);
        return entity.getId();
    }

    @Override
    @Transactional(rollbackFor = Exception.class)
    public boolean update(AdBo bo) {
        if (bo.getId() == null) {
            throw ServiceException.of("ID不能为空");
        }
        if (!adDao.exists(bo.getId())) {
            throw ServiceException.of("记录不存在");
        }
        Ad entity = MapstructUtils.convert(bo, Ad.class);
        beforeSave(entity);
        return adDao.updateById(entity);
    }

    @Override
    @Transactional(rollbackFor = Exception.class)
    public boolean batchDelete(Collection<Long> ids) {
        if (CollUtil.isEmpty(ids)) {
            throw ServiceException.of("ID集合不能为空");
        }
        beforeDelete(ids);  // 删除前钩子
        return adDao.deleteByIds(ids);
    }

    /**
     * 保存前钩子 - 子类可重写实现自定义逻辑
     */
    protected void beforeSave(Ad entity) {
        // 数据校验、默认值设置等
    }

    /**
     * 删除前钩子 - 子类可重写实现自定义逻辑
     */
    protected void beforeDelete(Collection<Long> ids) {
        // 关联数据校验、清理等
    }
}
```

**关键点**:
- ❌ **不继承任何基类** (这是与 ruoyi-vue-plus 的核心区别)
- ✅ 只注入 DAO,不注入 Mapper
- ✅ 通过 `adDao.buildQueryWrapper(bo)` 获取查询条件
- ✅ 使用 `MapstructUtils.convert()` 进行对象转换
- ✅ 提供 `beforeSave()` 和 `beforeDelete()` 钩子方法

### 3. DAO 层 (本项目独有)

**职责**: 构建查询条件、提供CRUD方法

#### DAO 接口

```java
package plus.ruoyi.business.base.dao;

import plus.ruoyi.business.base.domain.Ad;
import plus.ruoyi.business.base.domain.bo.AdBo;
import plus.ruoyi.common.mybatis.core.dao.IBaseDao;
import plus.ruoyi.common.mybatis.core.query.PlusLambdaQuery;

public interface IAdDao extends IBaseDao<Ad> {

    /**
     * 根据业务对象构建查询条件
     * ⭐ 这是本项目的核心设计 - 所有查询条件在DAO层构建
     */
    PlusLambdaQuery<Ad> buildQueryWrapper(AdBo bo);
}
```

#### DAO 实现类

```java
package plus.ruoyi.business.base.dao.impl;

import org.springframework.stereotype.Repository;
import plus.ruoyi.business.base.dao.IAdDao;
import plus.ruoyi.business.base.domain.Ad;
import plus.ruoyi.business.base.domain.bo.AdBo;
import plus.ruoyi.business.base.mapper.AdMapper;
import plus.ruoyi.common.core.utils.StringUtils;
import plus.ruoyi.common.mybatis.core.dao.impl.BaseDaoImpl;
import plus.ruoyi.common.mybatis.core.query.PlusLambdaQuery;
import java.util.Map;

@Repository
public class AdDaoImpl extends BaseDaoImpl<AdMapper, Ad> implements IAdDao {

    @Override
    public PlusLambdaQuery<Ad> buildQueryWrapper(AdBo bo) {
        Map<String, Object> params = bo.getParams();
        PlusLambdaQuery<Ad> lqw = PlusLambdaQuery.of();

        // ✅ 精确匹配 - 自动处理null值,无需判空
        lqw.eq(Ad::getId, bo.getId());
        lqw.eq(Ad::getStatus, bo.getStatus());
        lqw.like(Ad::getAdName, bo.getAdName());

        // ✅ 时间范围查询
        lqw.between(Ad::getCreateTime,
            params.get("beginCreateTime"),
            params.get("endCreateTime"));

        // ✅ 模糊搜索多个字段（跨数据库兼容）
        // String 类型用 like()，非 String 类型用 likeCast()
        String searchValue = bo.getSearchValue();
        if (StringUtils.isNotBlank(searchValue)) {
            lqw.and(w -> w
                .likeCast(Ad::getId, searchValue)          // Long 类型 → likeCast
                .or().like(Ad::getAdName, searchValue)     // String 类型 → like
                .or().like(Ad::getDescription, searchValue) // String 类型 → like
                .or().likeCast(Ad::getSortOrder, searchValue) // Long 类型 → likeCast
                .or().likeCast(Ad::getCreateTime, searchValue) // Date 类型 → likeCast
            );
        }

        return lqw;
    }
}
```

**关键点**:
- ✅ 继承 `BaseDaoImpl<Mapper, Entity>`
- ✅ 实现 `buildQueryWrapper(Bo)` 方法
- ✅ `PlusLambdaQuery` 自动处理null值,无需判空
- ✅ 提供完整的CRUD方法 (继承自BaseDaoImpl)
- ✅ **模糊查询必须区分字段类型**: String 用 `like()`，非 String 用 `likeCast()`

### 4. Mapper 层

**职责**: 纯数据访问

```java
package plus.ruoyi.business.base.mapper;

import com.baomidou.mybatisplus.core.mapper.BaseMapper;
import plus.ruoyi.business.base.domain.Ad;

public interface AdMapper extends BaseMapper<Ad> {
    // ✅ 只继承BaseMapper,不继承BaseMapperPlus
    // ✅ 无需额外方法,BaseDaoImpl已提供完整CRUD
}
```

**关键点**:
- ✅ 只继承 `BaseMapper<Entity>`
- ❌ 不继承 `BaseMapperPlus`
- ✅ 无需额外方法

---

## 领域模型设计

### 1. Entity 实体类

```java
package plus.ruoyi.business.base.domain;

import plus.ruoyi.common.tenant.core.TenantEntity;  // ✅ 继承TenantEntity
import com.baomidou.mybatisplus.annotation.*;
import lombok.Data;
import lombok.EqualsAndHashCode;
import java.io.Serial;

@Data
@EqualsAndHashCode(callSuper = true)
@TableName("b_ad")
public class Ad extends TenantEntity {

    @Serial
    private static final long serialVersionUID = 1L;

    @TableId(value = "id")
    private Long id;

    private String adName;
    private String status;

    @TableLogic
    private String isDeleted;
}
```

### 2. BO 业务对象

```java
package plus.ruoyi.business.base.domain.bo;

import io.github.linpeilie.annotations.AutoMappers;  // ✅ 自动映射
import io.github.linpeilie.annotations.AutoMapper;
import plus.ruoyi.business.base.domain.Ad;
import plus.ruoyi.business.base.domain.vo.AdVo;
import plus.ruoyi.common.mybatis.core.domain.BaseEntity;
import plus.ruoyi.common.core.validate.AddGroup;
import plus.ruoyi.common.core.validate.EditGroup;
import lombok.Data;
import lombok.EqualsAndHashCode;
import jakarta.validation.constraints.*;

@Data
@EqualsAndHashCode(callSuper = true)
@AutoMappers({  // ✅ 自动映射配置
    @AutoMapper(target = Ad.class, reverseConvertGenerate = false),
    @AutoMapper(target = AdVo.class)
})
public class AdBo extends BaseEntity {

    @NotNull(message = "主键id不能为空", groups = { EditGroup.class })
    private Long id;

    @NotBlank(message = "广告名称不能为空", groups = { AddGroup.class, EditGroup.class })
    private String adName;

    private String status;
}
```

### 3. VO 视图对象

```java
package plus.ruoyi.business.base.domain.vo;

import plus.ruoyi.business.base.domain.Ad;
import cn.idev.excel.annotation.ExcelIgnoreUnannotated;
import cn.idev.excel.annotation.ExcelProperty;
import io.github.linpeilie.annotations.AutoMapper;
import lombok.Data;
import java.io.Serial;
import java.io.Serializable;
import java.util.Date;

@Data
@ExcelIgnoreUnannotated
@AutoMapper(target = Ad.class)
public class AdVo implements Serializable {

    @Serial
    private static final long serialVersionUID = 1L;

    @ExcelProperty(value = "主键id")
    private Long id;

    @ExcelProperty(value = "广告名称")
    private String adName;

    @ExcelProperty(value = "状态")
    private String status;

    @ExcelProperty(value = "创建时间")
    private Date createTime;
}
```

---

## 核心工具类

### 对象转换

```java
import plus.ruoyi.common.core.utils.MapstructUtils;

// Entity -> VO
AdVo vo = MapstructUtils.convert(entity, AdVo.class);

// BO -> Entity
Ad entity = MapstructUtils.convert(bo, Ad.class);

// List转换
List<AdVo> voList = MapstructUtils.convert(entityList, AdVo.class);
```

### 异常处理

```java
import plus.ruoyi.common.core.exception.ServiceException;

// 抛出业务异常
throw ServiceException.of("广告配置不存在");

// 带参数的异常
throw ServiceException.of("广告 {} 不存在", adName);
```

### 字符串工具

```java
import plus.ruoyi.common.core.utils.StringUtils;

// 判空
if (StringUtils.isNotBlank(name)) {
    // ...
}

// 判空
if (StringUtils.isBlank(name)) {
    // ...
}
```

### 集合工具

```java
import cn.hutool.core.collection.CollUtil;

// 判空
if (CollUtil.isEmpty(list)) {
    // ...
}

// 判非空
if (CollUtil.isNotEmpty(list)) {
    // ...
}
```

---

## 开发规范

### 1. 命名规范

- **包名**: `plus.ruoyi.business.模块.子模块`
- **Entity**: 单数名词,如 `Ad`
- **BO**: Entity名 + `Bo`,如 `AdBo`
- **VO**: Entity名 + `Vo`,如 `AdVo`
- **Service接口**: `I` + Entity名 + `Service`,如 `IAdService`
- **Service实现**: Entity名 + `ServiceImpl`,如 `AdServiceImpl`
- **DAO接口**: `I` + Entity名 + `Dao`,如 `IAdDao`
- **DAO实现**: Entity名 + `DaoImpl`,如 `AdDaoImpl`
- **Mapper**: Entity名 + `Mapper`,如 `AdMapper`

### 2. 注解规范

```java
// Controller
@Validated                  // 参数校验
@RequiredArgsConstructor    // Lombok构造注入
@RestController            // REST控制器
@RequestMapping("/base/ad") // 请求路径

// Service
@Service                   // Spring服务
@RequiredArgsConstructor   // Lombok构造注入
@Transactional            // 事务注解(增删改方法)

// DAO
@Repository               // Spring数据访问

// Mapper
// 无需额外注解
```

### 3. 权限控制

```java
@SaCheckPermission("base:ad:query")   // 查询权限
@SaCheckPermission("base:ad:add")     // 新增权限
@SaCheckPermission("base:ad:update")  // 修改权限
@SaCheckPermission("base:ad:delete")  // 删除权限
@SaCheckPermission("base:ad:export")  // 导出权限
@SaCheckPermission("base:ad:import")  // 导入权限
```

### 4. 日志记录

```java
@Log(title = "广告配置", operType = DictOperType.INSERT)  // 新增
@Log(title = "广告配置", operType = DictOperType.UPDATE)  // 修改
@Log(title = "广告配置", operType = DictOperType.DELETE)  // 删除
@Log(title = "广告配置", operType = DictOperType.EXPORT)  // 导出
@Log(title = "广告配置", operType = DictOperType.IMPORT)  // 导入
```

---

## 与 ruoyi-vue-plus 的对比

| 特性 | ruoyi-vue-plus | 本项目 (ruoyi-plus-uniapp) |
|------|---------------|---------------------------|
| 包名 | `com.ruoyi` | `plus.ruoyi` |
| Service继承 | 继承 `ServiceImpl` | ❌ **不继承任何基类** |
| DAO层 | 无独立DAO层 | ✅ **独立DAO层 + buildQueryWrapper** |
| 查询构建 | Service层直接构建 | ✅ **DAO层构建** |
| Mapper继承 | 继承 `BaseMapperPlus` | 继承 `BaseMapper` |
| Entity继承 | 继承 `BaseEntity` | 继承 `TenantEntity` |
| BO映射 | 手动配置 | ✅ **@AutoMappers 自动** |
| 对象转换 | `BeanUtil` | ✅ **MapstructUtils** |

---

## 常见问题

### Q1: 为什么Service不继承基类?

**答**: 为了更灵活的业务逻辑实现,避免继承带来的限制。通过注入DAO来获取数据访问能力,而不是通过继承。

### Q2: 为什么要有独立的DAO层?

**答**:
1. **职责分离**: Service负责业务逻辑,DAO负责数据访问
2. **查询复用**: `buildQueryWrapper()` 方法可以在多个地方复用
3. **易于测试**: DAO层可以独立测试
4. **代码清晰**: 查询条件集中在一个地方,易于维护

### Q3: Entity为什么继承TenantEntity?

**答**: `TenantEntity` 包含了租户ID字段和审计字段,支持租户隔离和数据审计。

### Q4: 如何进行跨表查询?

**答**: 在Service层注入多个DAO,分别获取数据后组装:

```java
@Service
@RequiredArgsConstructor
public class OrderServiceImpl implements IOrderService {
    private final IOrderDao orderDao;
    private final IUserDao userDao;
    private final IProductDao productDao;

    @Override
    public OrderDetailVo getDetail(Long orderId) {
        // 获取订单信息
        Order order = orderDao.getById(orderId);

        // 获取用户信息
        User user = userDao.getById(order.getUserId());

        // 获取商品信息
        Product product = productDao.getById(order.getProductId());

        // 组装返回
        OrderDetailVo vo = new OrderDetailVo();
        vo.setOrder(MapstructUtils.convert(order, OrderVo.class));
        vo.setUser(MapstructUtils.convert(user, UserVo.class));
        vo.setProduct(MapstructUtils.convert(product, ProductVo.class));
        return vo;
    }
}
```

---

## 最佳实践

1. ✅ 写代码前先 Read 本项目现有代码
2. ✅ Service 不继承任何基类
3. ✅ DAO 层必须实现 `buildQueryWrapper()` 方法
4. ✅ Entity 继承 `TenantEntity`
5. ✅ BO 使用 `@AutoMappers` 注解
6. ✅ 使用 `MapstructUtils.convert()` 转换对象
7. ✅ 使用 `ServiceException.of()` 抛出业务异常
8. ✅ **先 import 完整类路径，再使用短类名**
9. ❌ 禁止在代码中使用完整包名（如 `plus.ruoyi.xxx.ClassName`）
10. ❌ 禁止使用 `Map<String, Object>` 传递业务数据
11. ❌ 禁止参考 ruoyi-vue-plus 的代码风格