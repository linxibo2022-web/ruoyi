---
name: e2e-test-mobile
description: |
  当需要用 aicoder 内置浏览器对移动端（plus-uniapp 的 H5 端）做端到端自动化测试、业务验收、回归测试时自动使用此 Skill。移动端通过启动 H5 端后在浏览器里测试。

  触发场景：
  - 开发完一个移动端页面/功能后，要在 H5 里验收（测新加业务）
  - 需要把移动端某条业务流（浏览-下单-支付等）跑一遍并产出报告（测完整业务）
  - 需要回归测试移动端，逐个用例落实、记录 Bug
  - 需要验证 WD UI 页面渲染、wd-paging 列表加载、接口返回、控制台无报错
  - 需要移动端登录态注入/绕过验证码做自动化

  触发词：移动端测试、H5测试、小程序网页版测试、plus-uniapp测试、移动端自动化、移动端E2E、移动端回归、移动端验收、wd-paging测试、WD UI测试、移动端冒烟、aicoder浏览器移动端、uniapp测试、移动端跑一遍
---
# e2e-test-mobile

## 执行边界

- 本入口只保留触发说明、最小执行原则与资料索引；开始实质实施前，先识别任务涉及的专题，再按需阅读对应完整资料。
- 项目根规则中的中文、编码、安全、架构及并发保护要求始终优先；不得因资料拆分降低既有约束。
- 不需要完整资料的只读解释、状态查询或简单定位，不得默认加载全文。

## 最小步骤

1. 根据当前任务确定所需专题。
2. 按需读取 `references/full-guide.md` 中相关章节，并遵守其中原有硬约束。
3. 仅在任务范围内实施并执行受影响范围的验证。

## 资料索引

- `references/full-guide.md`：原入口的完整规范、模板、案例和边界。主要专题：
- 概述
- 一、能力与边界
- 二、移动端 H5 的三个关键差异（务必先记住）
- 三、aicoder 浏览器工具速查（移动端测试视角）
- 四、登录态获取（两条路径）
- 五、移动端测试配方
- 六、五维断言模式（同 PC，缺一不算通过）
- 七、与 task-tracker 集成：测试记录结构
- 用例清单
- Bug 记录
- 执行结果
- 八、"逐个测试逐个落实"执行循环（核心工作方式）
- 九、两大场景的落地流程
- 十、常见坑（务必避免）
- 十一、端到端实操示例（首页列表 + 上拉加载，节选）
- 十二、技能产出清单
