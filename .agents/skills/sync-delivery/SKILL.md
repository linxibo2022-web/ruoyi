---
name: sync-delivery
description: |
  当用户用 `/sync-delivery` 命令或自然语言要求"把代码同步到交付目录、做交付副本、更新交付版本"时使用此 Skill。把主项目镜像同步到同级交付目录，产出不带 git 历史、不带技能体系、不带内部协作文档的可交付版本。

  触发场景：
  - 用户输入 `/sync-delivery`、`/sync-delivery --first-time`、`/sync-delivery --status` 等命令
  - 用户说"把代码同步到交付目录"
  - 用户说"做一份交付副本"或"更新交付版本"
  - 用户说"看看交付目录还差几个提交"
  - 需要触发主项目根的 Python 同步脚本生成交付物

  触发词：/sync-delivery、sync-delivery、交付同步、同步交付、同步到交付、交付副本、更新交付、deliver、delivery、release-copy
---
# sync-delivery

## 执行边界

- 本入口只保留触发说明、最小执行原则与资料索引；开始实质实施前，先识别任务涉及的专题，再按需阅读对应完整资料。
- 项目根规则中的中文、编码、安全、架构及并发保护要求始终优先；不得因资料拆分降低既有约束。
- 不需要完整资料的只读解释、状态查询或简单定位，不得默认加载全文。

## 最小步骤

1. 根据当前任务确定所需专题。
2. 按需读取 `references/full-guide.md` 中相关章节，并遵守其中原有硬约束。
3. 仅在任务范围内实施并执行受影响范围的验证。

## 资料索引

- `references/full-guide.md`：原入口的完整规范、模板、案例和边界。主要专题：
- 触发方式
- 执行流程
- 内部约定
- 详细规范
- 关联命令
