---
name: add-test
description: |
  当需要为已有业务模块补充测试文件时使用。支持 ServiceTest、ControllerTest、E2E 配置、前端 Vitest 的智能生成。

  触发场景：
  - 已有模块缺少测试，需要一次性补充
  - 模块新增了 composable 或 utils，需要补 Vitest
  - CI 报告覆盖率不足，需要补充特定类型的测试
  - 只缺某一种测试（如只加 E2E），需要单独补充

  触发词：/add-test、补测试、加测试、补充测试、生成测试、缺少测试、测试覆盖、Vitest、ServiceTest、ControllerTest
---
# add-test

## 执行边界

- 本入口只保留触发说明、最小执行原则与资料索引；开始实质实施前，先识别任务涉及的专题，再按需阅读对应完整资料。
- 项目根规则中的中文、编码、安全、架构及并发保护要求始终优先；不得因资料拆分降低既有约束。
- 不需要完整资料的只读解释、状态查询或简单定位，不得默认加载全文。

## 最小步骤

1. 根据当前任务确定所需专题。
2. 按需读取 `references/full-guide.md` 中相关章节，并遵守其中原有硬约束。
3. 仅在任务范围内实施并执行受影响范围的验证。

## 资料索引

- `references/full-guide.md`：原入口的完整规范、模板、案例和边界。主要专题：
- 用途
- 调用方式
- 智能检测规则
- 防护规则
