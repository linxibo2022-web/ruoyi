
# 任务跟踪技能

## 何时使用

✅ 用户明确要求跟踪任务（"创建任务跟踪"、"记录进度"、"跟踪这个任务"）
✅ 复杂的多步骤开发任务
✅ 需要跨会话恢复的工作

❌ 简单的单步操作
❌ 纯咨询性对话

## 目录结构

```
{项目根目录}/
└── docs/
    └── tasks/
        ├── README.md          # 使用说明（首次自动生成）
        ├── active/            # 进行中的任务
        │   └── task-20260121-153045-用户反馈.md
        └── archive/           # 已完成的任务
            └── 2026-01/
                └── task-20260120-优惠券系统.md
```

## 文档模板

```markdown
# 任务：{任务标题}

**状态**: 🟢 进行中 | 🔵 已完成 | 🔴 已暂停
**创建时间**: {YYYY-MM-DD HH:MM:SS}
**更新时间**: {YYYY-MM-DD HH:MM:SS}
**Git 分支**: {branch_name}

---

## 📋 需求描述

{用户原始需求的详细描述，包括背景、目标、约束条件}

---

## 🎨 方案设计

### 方案讨论

#### 方案 A：{方案名称}
- **描述**: {方案简述}
- **优点**:
  - {优点1}
  - {优点2}
- **缺点**:
  - {缺点1}
  - {缺点2}
- **适用场景**: {什么情况下适合}
- **技术栈**: {需要用到的技术/框架}

#### 方案 B：{方案名称}
- **描述**: {方案简述}
- **优点**:
  - {优点1}
- **缺点**:
  - {缺点1}
- **适用场景**: {什么情况下适合}
- **技术栈**: {需要用到的技术/框架}

### 最终选择

**选定方案**: 方案 A

**选择理由**:
1. {理由1：如性能更好}
2. {理由2：如更符合项目规范}
3. {理由3：如维护成本更低}

**权衡考量**:
- {权衡点1：如牺牲了一定的灵活性，换取了性能}
- {权衡点2：如需要额外的学习成本}

**备选方案**:
- 如果 {某种情况发生}，可以考虑切换到方案 B

---

## 🔍 技术调研

### 技术选型

| 技术/框架 | 版本 | 优势 | 劣势 | 是否采用 | 备注 |
|----------|------|------|------|---------|------|
| {技术A}   | x.x  | {优势} | {劣势} | ✅ | {为什么选} |
| {技术B}   | x.x  | {优势} | {劣势} | ❌ | {为什么不选} |
| {技术C}   | x.x  | {优势} | {劣势} | 🤔 | {待评估} |

### 调研结论

{调研的总结性结论，包括：}
- **推荐方案**: {最终选择的技术方案}
- **关键依赖**: {需要引入的核心依赖}
- **风险评估**: {潜在的技术风险}
- **学习成本**: {团队需要的学习时间}

### 参考资料

- [{文档名称}]({链接}) - {说明}
- [{博客标题}]({链接}) - {说明}

---

## 🎯 实现步骤

- [ ] 1. {步骤1简述}
  - **文件**: `path/to/file.ext`
  - **说明**: {详细说明}
  - **依赖**: {需要先完成什么}
  - **预期结果**: {完成后应该达到什么状态}

- [x] 2. {步骤2简述}
  - **文件**: `another/file.ext`
  - **说明**: {详细说明}
  - **依赖**: {需要先完成什么}
  - **预期结果**: {完成后应该达到什么状态}
  - **完成时间**: {YYYY-MM-DD HH:MM}
  - **实际结果**: {实际完成的情况}

- [ ] 3. {步骤3简述}

---

## 📝 关键决策

- **模块归属**: {模块名称，如 business.base}
- **数据库表**: {表名前缀，如 b_}
- **主键策略**: {ID生成方式，如雪花ID}
- **技术选型**: {框架/库版本}
- **架构模式**: {如四层架构、MVC等}
- **缓存策略**: {是否使用缓存，用什么缓存}

---

## 🐛 问题记录

### 问题 1: {问题简述}
- **发现时间**: {YYYY-MM-DD HH:MM}
- **影响范围**: {影响的功能/模块}
- **问题描述**: {详细描述问题现象}
- **根本原因**: {问题的根源是什么}
- **解决方案**: {如何解决的}
- **状态**: 🟢 已解决 | 🟡 进行中 | 🔴 待解决
- **解决时间**: {YYYY-MM-DD HH:MM}（如果已解决）

### 问题 2: {问题简述}
- **发现时间**: {YYYY-MM-DD HH:MM}
- **影响范围**: {影响的功能/模块}
- **问题描述**: {详细描述}
- **根本原因**: {根源}
- **解决方案**: {方案}
- **状态**: 🟢 已解决 | 🟡 进行中 | 🔴 待解决

---

## 🔄 当前进度

**已完成**: {X} / {总数} 步骤 ({百分比}%)

**当前状态**:
- **正在进行**: {当前正在做的具体事情}
- **最后更新**: {上次做了什么，完成了什么}
- **遇到的问题**: {当前遇到的问题，如果有}

**下一步操作**:
1. {下一步具体要做的事情}
   - **依赖**: {需要什么前置条件}
   - **预期结果**: {完成后应该达到什么状态}
   - **预计耗时**: {大概需要多久}
2. {再下一步}
   - **依赖**: {需要什么前置条件}
   - **预期结果**: {完成后应该达到什么状态}

**待决策事项**:
- [ ] {需要决策的事情1，如：选择使用 Redis 还是本地缓存}
- [ ] {需要决策的事情2，如：是否需要增加权限控制}

**阻塞问题**:
- {如果有阻塞当前进度的问题，在这里列出}

---

## 📁 相关文件

- `{文件路径}` - {文件用途}
- `{文件路径}` - {文件用途}

---

## ⚠️ 注意事项

- {需要特别注意的点}
- {已知问题或风险}
- {性能考量}
- {安全性考量}

---

## 💬 变更记录

### {YYYY-MM-DD HH:MM}
**变更类型**: 进度更新 | 方案调整 | 问题修复 | 决策变更

**变更内容**:
- {本次更新的详细内容}
- {如果是方案调整，说明为什么调整}
- {如果是决策变更，说明变更理由}

**影响范围**:
- {这次变更影响了哪些部分}
```

## 核心操作

### 1. 创建任务

**触发**：用户说"创建任务跟踪"、"记录这个任务"

**步骤**：
```bash
# 1. 确保目录存在
mkdir -p docs/tasks/active docs/tasks/archive

# 2. 生成文件名
# 格式: task-{YYYYMMdd-HHMMSS}-{任务简称}.md
TIMESTAMP=$(TZ=Asia/Shanghai date +%Y%m%d-%H%M%S)
SLUG="{任务标题简化版，截断30字符}"
FILENAME="task-${TIMESTAMP}-${SLUG}.md"

# 3. 创建文档
# 填充上述模板内容到 docs/tasks/active/$FILENAME

# 4. 首次使用时创建 README.md（如不存在）
```

**README.md 内容**：
```markdown
# 任务跟踪中心

## 目录说明
- `active/` - 进行中的任务
- `archive/` - 已完成的任务（按月归档）

## 使用方法
1. Claude 自动创建任务跟踪文档
2. 手动编辑文档更新进度（修改复选框）
3. 任务完成后移动到 archive/{年-月}/

## 快速恢复
```bash
# 查看活跃任务
ls -1 active/

# 恢复任务
cat active/task-20260121-*.md
```

### 2. 更新进度

**触发**：用户说"更新进度"、"标记步骤X完成"

**步骤**：
```bash
# 1. 找到当前任务文档（最新的那个）
TASK_FILE=$(ls -t docs/tasks/active/*.md | head -1)

# 2. 更新复选框: - [ ] → - [x]
sed -i "s/^- \[ \] ${STEP_NUMBER}\./- [x] ${STEP_NUMBER}./" "$TASK_FILE"

# 3. 更新时间戳
CURRENT_TIME=$(TZ=Asia/Shanghai date '+%Y-%m-%d %H:%M:%S')
sed -i "s/^\*\*更新时间\*\*:.*/\*\*更新时间\*\*: $CURRENT_TIME/" "$TASK_FILE"

# 4. 添加变更记录
echo -e "### $CURRENT_TIME\n- 完成步骤 $STEP_NUMBER: $DESCRIPTION" >> "$TASK_FILE"

# 5. 重新计算进度百分比
TOTAL=$(grep -c "^- \[.\]" "$TASK_FILE")
DONE=$(grep -c "^- \[x\]" "$TASK_FILE")
PERCENT=$((DONE * 100 / TOTAL))
```

### 3. 列出活跃任务

**触发**：用户说"有哪些任务"、"列出任务"

**步骤**：
```bash
for file in docs/tasks/active/*.md; do
  TITLE=$(grep "^# 任务：" "$file" | sed 's/^# 任务：//')
  STATUS=$(grep "^\*\*状态\*\*:" "$file" | sed 's/.*: //')
  PROGRESS=$(grep "^\*\*已完成\*\*:" "$file" | sed 's/.*: //')
  
  echo "📄 $TITLE"
  echo "   状态: $STATUS | 进度: $PROGRESS"
  echo "   文件: $file"
  echo ""
done
```

### 4. 恢复任务

**触发**：用户说"继续上次的任务"、"恢复任务"

**步骤**：
```bash
# 1. 确定要恢复的任务
if [ 用户指定了任务名 ]; then
  TASK_FILE=$(find docs/tasks/active -name "*{用户指定的关键词}*.md" | head -1)
else
  TASK_FILE=$(ls -t docs/tasks/active/*.md | head -1)  # 最新的
fi

# 2. 读取文档内容
cat "$TASK_FILE"

# 3. 提取关键信息并向用户确认
TITLE=$(grep "^# 任务：" "$TASK_FILE" | sed 's/^# 任务：//')
NEXT_STEPS=$(提取 "下一步操作" 部分)

# 4. 输出恢复信息
echo "正在恢复任务：$TITLE"
echo "下一步操作："
echo "$NEXT_STEPS"
```

### 5. 记录方案讨论

**触发**：用户说"记录方案讨论"、"添加方案"、"方案对比"、"这个方案"

**步骤**：
```bash
# 1. 找到当前任务文档
TASK_FILE=$(ls -t docs/tasks/active/*.md | head -1)

# 2. 在"方案设计"部分添加新方案
# 如果是添加新方案，在"方案讨论"下增加方案 C、D 等
# 如果是记录选择理由，更新"最终选择"部分

# 3. 更新变更记录
CURRENT_TIME=$(TZ=Asia/Shanghai date '+%Y-%m-%d %H:%M')
echo -e "### $CURRENT_TIME\n**变更类型**: 方案讨论\n\n**变更内容**:\n- 添加方案讨论: $DESCRIPTION" >> "$TASK_FILE"
```

**操作指南**：
1. 如果有多个方案，逐个记录每个方案的优缺点
2. 记录方案对比的维度（性能、成本、复杂度等）
3. 明确记录选择理由和权衡考量
4. 保留备选方案，方便后续调整

### 6. 记录技术调研

**触发**：用户说"记录技术调研"、"添加技术选型"、"技术对比"

**步骤**：
```bash
# 1. 找到当前任务文档
TASK_FILE=$(ls -t docs/tasks/active/*.md | head -1)

# 2. 在"技术调研"部分更新技术选型表格
# 添加新的技术对比行

# 3. 更新调研结论

# 4. 添加参考资料链接

# 5. 更新变更记录
CURRENT_TIME=$(TZ=Asia/Shanghai date '+%Y-%m-%d %H:%M')
echo -e "### $CURRENT_TIME\n**变更类型**: 技术调研\n\n**变更内容**:\n- 完成技术调研: $DESCRIPTION" >> "$TASK_FILE"
```

**操作指南**：
1. 对比至少 2-3 个技术方案
2. 记录版本号（避免未来升级时的兼容性问题）
3. 评估学习成本和风险
4. 保存参考资料链接，方便后续查阅

### 7. 记录问题

**触发**：用户说"记录问题"、"遇到问题"、"添加问题"

**步骤**：
```bash
# 1. 找到当前任务文档
TASK_FILE=$(ls -t docs/tasks/active/*.md | head -1)

# 2. 在"问题记录"部分添加新问题
CURRENT_TIME=$(TZ=Asia/Shanghai date '+%Y-%m-%d %H:%M')

# 3. 记录问题的完整信息：
#    - 问题描述
#    - 根本原因
#    - 解决方案
#    - 当前状态

# 4. 如果问题已解决，标记解决时间

# 5. 更新变更记录
echo -e "### $CURRENT_TIME\n**变更类型**: 问题记录\n\n**变更内容**:\n- 记录问题: $PROBLEM_TITLE\n- 状态: $STATUS" >> "$TASK_FILE"
```

**操作指南**：
1. 及时记录问题，避免遗忘细节
2. 详细描述问题现象和影响范围
3. 分析根本原因，不只是表面现象
4. 记录解决方案的详细步骤
5. 问题解决后及时更新状态

### 8. 归档任务

**触发**：用户说"任务完成"、"归档任务"

**步骤**：
```bash
# 1. 更新状态
sed -i "s/^\*\*状态\*\*:.*/\*\*状态\*\*: 🔵 已完成/" "$TASK_FILE"

# 2. 移动到归档目录
YEAR_MONTH=$(TZ=Asia/Shanghai date +%Y-%m)
mkdir -p "docs/tasks/archive/$YEAR_MONTH"
mv "$TASK_FILE" "docs/tasks/archive/$YEAR_MONTH/"

echo "✅ 已归档到: docs/tasks/archive/$YEAR_MONTH/"
```

## 使用示例

**场景1：创建任务**
```
用户: "创建一个用户反馈功能的任务跟踪"

Claude:
✅ 创建文档: docs/tasks/active/task-20260121-153045-用户反馈.md
📋 已记录需求描述
🎯 列出5个实现步骤
💡 开始第一步：设计数据库表结构
```

**场景2：更新进度**
```
用户: "标记步骤1和2完成"

Claude:
✅ 已更新进度: 2/5 (40%)
⏰ 更新时间: 2026-01-21 16:45:30
📌 下一步: 实现 Service 层
```

**场景3：记录方案讨论**
```
用户: "我在考虑用 Redis 缓存还是本地缓存，帮我记录一下方案对比"

Claude:
📝 已记录方案对比到任务文档
🎨 方案设计部分已更新：
  - 方案 A：Redis 分布式缓存
    优点：多实例共享、数据持久化
    缺点：增加部署复杂度、网络开销
  - 方案 B：本地 Caffeine 缓存
    优点：性能更好、无需额外部署
    缺点：多实例不一致、重启丢失

💡 建议：如果是单机部署，推荐方案 B；如果是集群，推荐方案 A

📌 请确认选择哪个方案，我会记录到文档中
```

**场景4：记录技术调研**
```
用户: "记录一下定时任务的技术调研结果"

Claude:
🔍 已更新技术调研部分：

| 技术/框架    | 版本  | 优势               | 劣势           | 是否采用 |
|-------------|-------|-------------------|----------------|---------|
| SnailJob    | 1.0.0 | 分布式、可视化管理  | 需要独立部署    | ✅      |
| @Scheduled  | 内置  | 简单、无需额外组件  | 功能有限        | ❌      |
| XXL-Job     | 2.x   | 功能强大          | 依赖过多        | ❌      |

📋 调研结论：选择 SnailJob，理由：
  1. 支持分布式集群
  2. 可视化管理界面
  3. 失败重试机制完善

📁 已保存到任务文档的"技术调研"部分
```

**场景5：记录问题**
```
用户: "遇到一个问题，Mapper 扫描不到，帮我记录一下"

Claude:
🐛 已记录问题到任务文档：

**问题**: Mapper 接口扫描不到
- **发现时间**: 2026-01-21 17:30
- **影响范围**: DAO 层无法注入
- **根本原因**: 缺少 @MapperScan 注解
- **解决方案**: 在启动类添加 @MapperScan("plus.ruoyi.business.*.mapper")
- **状态**: 🟢 已解决

📌 已更新变更记录
```

**场景6：恢复任务（包含方案上下文）**
```
[用户重新打开窗口]
用户: "继续上次的用户反馈功能"

Claude:
📄 正在恢复任务：用户反馈管理功能

✅ 已完成: 数据库设计、实体类创建 (2/5，40%)

🎨 方案设计：
  - 选定方案：Redis 分布式缓存
  - 选择理由：
    1. 项目采用集群部署
    2. 需要多实例共享缓存
    3. Redis 已经在项目中使用

📌 下一步: 实现 Service 层
  - 依赖：Entity 和 DAO 已完成
  - 预期结果：完成 CRUD 业务逻辑

🐛 当前遇到的问题：无

🔗 文件: docs/tasks/active/task-20260121-153045-用户反馈.md
```

## 最佳实践

**应该做**：
- ✅ 一个任务 = 一个完整功能开发
- ✅ 及时更新进度（完成一步更新一次）
- ✅ 详细记录关键决策和选择理由
- ✅ 保留原始需求描述
- ✅ **记录方案讨论过程**（多个方案、对比、选择理由）
- ✅ **记录技术调研结果**（技术选型、对比、结论）
- ✅ **及时记录遇到的问题**（问题、原因、解决方案）
- ✅ **下一步操作要有上下文**（依赖、预期结果、为什么这样做）
- ✅ **变更记录要详细**（记录方案调整、决策变更的理由）

**不应该做**：
- ❌ 为每个小改动创建任务
- ❌ 删除旧任务（归档即可）
- ❌ 自动归档（由用户决定）
- ❌ **只记录最终方案，不记录讨论过程**
- ❌ **只记录步骤，不记录为什么这样做**
- ❌ **问题解决了就不记录**（应该记录问题和解决过程）

## 错误处理

**目录不存在** → 自动创建 `docs/tasks/active` 和 `archive`

**没有活跃任务** → 提示用户：创建新任务 | 恢复归档任务 | 查看历史

**文档格式错误** → 尽量解析，提示用户保持格式：
- 保持 `##` 标题层级
- 复选框格式：`- [ ]` 或 `- [x]`
- 不删除 `**字段**` 标记

## 功能边界

**适用于**：单人开发、本地存储、快速恢复上下文

**不适用于**：多人协作、实时同步、复杂依赖管理（请使用专业项目管理工具）
