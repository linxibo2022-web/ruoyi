---
name: deployment-guide
description: |
  当需要部署项目到生产环境、构建前端/移动端、管理安全密钥、配置 Docker 容器编排时自动使用此 Skill。

  触发场景：
  - 需要将项目部署到生产服务器（JAR 或 Docker 方式）
  - 需要使用一键自动化部署（deploy.py）
  - 需要构建前端（PC/H5/小程序/APP）发布包
  - 需要检查和更换生产环境安全密钥（JWT/RSA/AES）
  - 需要生成 Docker Compose 本地配置文件
  - 需要配置 Nginx 反向代理和负载均衡
  - 需要使用 1Panel 进行容器编排管理

  触发词：部署、上线、发布、生产环境、Docker、Compose、构建、build、打包、JAR、密钥、安全配置、Nginx、1Panel、容器编排、小程序发布、APP打包、H5部署、一键部署、deploy.py、自动部署、裸机、全新服务器、VPS、没装Docker、环境检测、环境探测、内存不足、OOM、低配服务器、Reeve
---
# deployment-guide

## 执行边界

- 本入口只保留触发说明、最小执行原则与资料索引；开始实质实施前，先识别任务涉及的专题，再按需阅读对应完整资料。
- 项目根规则中的中文、编码、安全、架构及并发保护要求始终优先；不得因资料拆分降低既有约束。
- 不需要完整资料的只读解释、状态查询或简单定位，不得默认加载全文。

## 最小步骤

1. 根据当前任务确定所需专题。
2. 按需读取 `references/full-guide.md` 中相关章节，并遵守其中原有硬约束。
3. 仅在任务范围内实施并执行受影响范围的验证。

## 资料索引

- `references/full-guide.md`：原入口的完整规范、模板、案例和边界。主要专题：
- 🔴 部署前置流程（最高优先级，必须先走这步再动手）
- 概述
- 一键自动化部署（推荐）
- 第一步：生产环境安全密钥检查（两种模式通用）
- 第二步：生产环境配置检查
- 第三步：后端构建
- 第四步-A：JAR 部署模式
- 第四步-B：Docker 部署模式
- 第五步：前端构建与部署
- 第六步：Nginx 配置
- 生产环境完整检查清单
- 常见问题
- 文件位置速查
