---
name: plan-executor
description: |
  当需要按已确认的实施计划自动执行任务、调度子 Agent、处理依赖与验收闭环时使用此 Skill。

  触发场景：
  - 需要执行 docs/tasks/active/ 中的实施计划
  - 需要由父 Agent 按依赖关系调度多个子 Agent
  - 需要自动修复编译、规范或测试失败并回写任务进度
  - 需要在不默认运行 E2E 的前提下完成任务验收

  触发词：执行实施计划、自动执行任务、计划执行、plan-executor、任务编排、父 Agent、子 Agent、依赖调度、并行开发、验收闭环
---
# plan-executor

## 执行边界

- 本入口只保留触发说明、最小执行原则与资料索引；开始实质实施前，先识别任务涉及的专题，再按需阅读对应完整资料。
- 项目根规则中的中文、编码、安全、架构及并发保护要求始终优先；不得因资料拆分降低既有约束。
- 不需要完整资料的只读解释、状态查询或简单定位，不得默认加载全文。

## 最小步骤

1. 根据当前任务确定所需专题。
2. 按需读取 `references/full-guide.md` 中相关章节，并遵守其中原有硬约束。
3. 仅在任务范围内实施并执行受影响范围的验证。

## 资料索引

- `references/full-guide.md`：原入口的完整规范、模板、案例和边界。主要专题：
- 目标与边界
- 输入前置检查
- 父 Agent 调度流程
- 临时产物生命周期与清理
- 串行与并行规则
- 验收重基线与失败处理
- 无进展与 Token 保护
- 自动裁决策略
- 低 Token 规则
- Goal 协作
- 执行报告
