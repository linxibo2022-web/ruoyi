---
name: module-strip
description: |
  当需要在「交付目录」里删除指定业务模块（如不要商城/IoT/支付/AI）时使用此 Skill。处理删除目录 + 修改 pom.xml 的语义级裁剪，是 delivery-sync 的下游补充技能。

  触发场景：
  - 需要从交付副本里去掉商城功能（mall）
  - 需要从交付副本里去掉物联网功能（iot）
  - 需要从交付副本里去掉支付功能（pay）
  - 需要从交付副本里去掉 AI 对话功能（ai）
  - 需要列出可裁剪的模块预设
  - 需要在裁剪后跑 mvn compile 验证仍可编译
  - 需要为客户定制不同的功能子集

  触发词：模块裁剪、裁剪模块、删除模块、移除模块、去掉模块、stripModules、module-strip、strip-modules、商城裁剪、IoT裁剪、支付裁剪、AI裁剪、不要mall、不要iot、不要pay、不要ai
---
# module-strip

## 执行边界

- 本入口只保留触发说明、最小执行原则与资料索引；开始实质实施前，先识别任务涉及的专题，再按需阅读对应完整资料。
- 项目根规则中的中文、编码、安全、架构及并发保护要求始终优先；不得因资料拆分降低既有约束。
- 不需要完整资料的只读解释、状态查询或简单定位，不得默认加载全文。

## 最小步骤

1. 根据当前任务确定所需专题。
2. 按需读取 `references/full-guide.md` 中相关章节，并遵守其中原有硬约束。
3. 仅在任务范围内实施并执行受影响范围的验证。

## 资料索引

- `references/full-guide.md`：原入口的完整规范、模板、案例和边界。主要专题：
- 概述
- 与 delivery-sync 的边界
- 文件结构
- 预设清单格式
- 触发命令
- 配置示例
- 工作流程
- 内置模块预设说明
- 用户决策点
- 不做的事（边界声明）
- 实战示例
- 添加新预设的流程
- 触发自检
- 与其他技能的关系
