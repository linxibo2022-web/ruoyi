# 技能双端差异与修复记录

检查时间：2026-08-19 16:59（Asia/Shanghai）  
检查基准：Codex（`.agents/skills/`）  
检查范围：全部技能、策略声明的 Claude Command → Codex Skill 映射

## 检查结论

修复后，全部共享技能包递归一致；16 个命令映射正文一致。`autonomous-dev`、`cmd-framework-sync` 仍按策略作为 Codex 端专属能力跳过检查，无未声明差异。

## 初始差异

| 类别 | 数量 | 受影响能力 |
| --- | ---: | --- |
| Codex 技能缺少 `references/full-guide.md` | 14 | add-test、add-todo、check、crud、dev、exp、init-docs、next、progress、start、strip-modules、sync、sync-delivery、update-status |
| YAML `name` 与目录名不一致 | 1 | deploy（`deploy-command` → `deploy`） |
| Claude Command 与 Codex Skill 正文不一致 | 1 | framework-sync |

## 修复内容

1. 为上述 16 个命令映射建立短入口与完整资料分层：Codex 技能入口保留触发说明、执行边界、最小步骤和资料索引；原有长正文无损归档至各自的 `references/full-guide.md`。
2. 在 `.claude/commands/<命令>-references/full-guide.md` 保存对应完整资料，并将 Claude Command 更新为与 Codex Skill 去除 YAML 后一致的短入口正文。
3. 修正 `deploy` 的 YAML `name` 为 `deploy`。
4. 按 Codex 基准同步 `framework-sync` 的 Claude Command 正文；其完整资料同时保留在双方资料目录中。
5. 新增 `add-skill/scripts/split-command-mapped-entries.cjs`，仅针对 `.agent-governance/skill-sync-policy.json` 显式声明的命令映射执行上述无损迁移，避免后续同类修复依赖人工批量编辑。

## 验证结果

以下命令均通过：

```powershell
node .claude/skills/skill-check/scripts/skill-check.cjs --base codex --fix-differences
node .claude/skills/skill-check/scripts/skill-check.cjs --base codex
node .agent-governance/scripts/verify-agent-assets.cjs
node .codex/hooks/test/skill-router.test.cjs
node .claude/hooks/test/skill-router.test.cjs
node .agent-governance/reports/run-regression.cjs
```

验收标准：无未声明技能差异、全部基准有效、命令映射一致、双端路由夹具通过、共享技能镜像哈希一致，且变更文件均为 UTF-8 无 BOM。
