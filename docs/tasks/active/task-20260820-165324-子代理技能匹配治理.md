# 任务：Claude Code 与 Codex 子代理技能匹配治理实施

**状态**：🟢 进行中
**创建时间**：2026-08-20 16:53:24（Asia/Shanghai）
**更新时间**：2026-08-20 16:53:24（Asia/Shanghai）
**Git 分支**：`feature/Mid-Platform`
**方案来源**：`docs/brainstorm-子代理技能匹配统一治理.md`
**关联基线**：`docs/tasks/active/task-20260819-101448-agent-context-optimization.md` 已完成的技能路由、短入口与双端同步能力

---

## 计划契约头

- **执行模式**：`dev-loop`
- **判定依据**：用户确认“先生成执行计划”，但未要求自动执行、`plan-executor`、并行子 Agent 或一次完成整个计划；因此本计划默认按依赖顺序逐项执行和回写，不声明可自动跑完整个计划。
- **目标**：让主代理每次创建 Claude Code/Codex 子代理前，针对具体子任务重新进行技能评估，并以可信、紧凑、可审计的信封传递结果；同时完成项目 agent 的跨端单源生成、风险门禁、上下文预算和安全迁移。
- **架构**：技能触发继续由 `.agent-governance/skills-manifest.json` 与现有 router 管理；项目 agent 元数据、风险级别和跨端能力引用由新增 `agents-manifest.json` 管理。公共 preflight、信封、registry 和启动上下文位于 `.agent-governance/lib/`，Claude/Codex Hook 仅做运行时字段适配。
- **技术栈**：Node.js CommonJS、JSON manifest/fixture、Claude Code hooks 与 agent Markdown、Codex hooks 与 agent TOML、现有同步/校验脚本。
- **模块归属**：Agent 治理基础设施；不属于业务模块。
- **端支持**：Claude Code、Codex。
- **数据库/表前缀**：不涉及。
- **非目标**：不修改业务代码、数据库、前后端页面；首版不实现强制 `selectAgent()`；不承诺多代理降低总 Token；Codex 递归深度未被真实运行时证明前不宣称硬门禁。
- **上位需求说明**：仓库当前不存在 `docs/需求文档.md`，本计划以已确认的 brainstorm 文档和当前仓库事实作为上位契约。

### 全局成功标准

1. 主会话原有 `UserPromptSubmit` 技能路由行为、44 个现有双端 fixture 和安全 Hook 不回归。
2. Claude `Agent.prompt` 与 Codex `spawn_agent.message` 均在创建前得到独立的任务级路由结果。
3. `matched`、`no-match`、`router-error`、`invalid-input` 可区分；只有通用只读探索可在错误时降级，高风险角色必须 deny。
4. 伪造、重复、截断或正文中的 `SUBAGENT_SKILL_ROUTE` 标记不能绕过权威重算。
5. `check` 在 Claude 解析为 Command、在 Codex 解析为 Skill；Claude Command 不进入 agent `skills:`。
6. Claude 递归委派真实 deny；Codex 依据 P0 探针结论选择硬门禁或可审计软约束，不伪造能力。
7. 新项目角色为 `ruoyi-code-reviewer`、`ruoyi-project-reporter`、`ruoyi-project-updater`；内置保留名不被覆盖。
8. 双端 agent 由同一治理源生成；运行时发现目录不会同时保留新旧项目角色。
9. Hook、agent、信封与回传摘要满足上下文预算，日志和审计不保存任务正文。
10. 全部静态门禁、双端 Hook 测试、staging 验收和重启后人工验收通过后才能完成任务。

### 初始上下文预算

| 项目 | 初始上限 | 说明 |
|---|---:|---|
| 单个技能路由信封 | 1024 UTF-8 字节 | `updatedInput` 中的任务原文不计入信封体积 |
| `SubagentStart.additionalContext` | 800 tokens | 与 `additionalContextLimit` 语义一致，另记录 UTF-8 字节数 |
| 单个中立 agent 正文 | 200 行 | 禁止复制技能全文和大型检查清单 |
| Claude 静态预载技能 | 0～2 个，合计不超过 8 KiB | 仅 `preloadable: true` 的 Skill；Command 不预载 |
| 子代理单次回传摘要 | 1500 个中文字符以内 | 只含结论、证据路径、风险和验证结果 |

P0 实测若证明某项预算不可行，可在不扩大主线程污染的前提下调整；必须记录基线、理由和新上限，不能静默放宽。

### 文件清单表

| 文件或目录 | 操作 | 负责内容 |
|---|---|---|
| `.agent-governance/agents-manifest.json` | 新增 | agent、风险级别、runtime 资源与预算唯一来源 |
| `.agent-governance/lib/router.cjs` | 修改 | 增加可诊断路由 API并保持旧 API 兼容 |
| `.agent-governance/lib/agent-registry.cjs` | 新增 | agent/能力解析、保留名和风险判定 |
| `.agent-governance/lib/delegation-envelope.cjs` | 新增 | 权威信封替换、渲染与校验 |
| `.agent-governance/lib/agent-preflight.cjs` | 新增 | 双端共享 preflight 决策 |
| `.agent-governance/lib/subagent-context.cjs` | 新增 | `SubagentStart` 公共上下文与预算 |
| `.agent-governance/fixtures/*.json` | 新增/修改 | 脱敏运行时、preflight、registry 和生成器样例 |
| `.agent-governance/agents/ruoyi-*.md` | 新增 | 三个运行时中立角色正文源 |
| `.agent-governance/scripts/sync-agent-assets.cjs` | 修改 | 根规则及双端 agent staging/生成/`--check` |
| `.agent-governance/scripts/verify-agent-assets.cjs` | 修改 | agent schema、资源、Hook、漂移、编码、预算门禁 |
| `.claude/hooks/agent-skill-preflight.cjs` | 新增 | Claude Agent 创建前适配 |
| `.claude/hooks/subagent-start.cjs` | 新增 | Claude 启动上下文适配 |
| `.claude/hooks/pre-tool-use.cjs` | 修改 | Windows PowerShell 安全检查与既有行为保持 |
| `.claude/settings.json` | 修改 | 合并 Agent/SubagentStart Hook，修正跨平台命令格式 |
| `.codex/hooks/agent-skill-preflight.cjs` | 新增 | Codex spawn_agent 创建前适配 |
| `.codex/hooks/subagent-start.cjs` | 新增 | Codex 启动上下文适配 |
| `.codex/hooks.json`、`.codex/config.toml` | 修改 | 合并 Hook、Git 根路径、Windows 命令与 agents 配置 |
| `.claude/agents/ruoyi-*.md` | 生成 | Claude 三个项目 agent |
| `.codex/agents/ruoyi-*.toml` | 生成 | Codex 三个项目 agent |
| `.agent-governance/core-rules.md` | 修改 | 精简全局委派硬原则 |
| `AGENTS.md`、`CLAUDE.md` | 生成 | 同步后的双端根规则，不手改 |
| `.claude/commands/dev-loop.md` | 修改 | 统一委派 contract 与回传格式 |
| `docs/AI体系学习笔记.md`、`docs/AI助手实战使用指南.md` | 修改 | 更新项目 agent 名称与职责 |

### 并发工作区保护

计划创建时以下文件已有未提交改动，执行任务时必须先查看 diff 并合并，不得覆盖：

- `.agent-governance/core-rules.md`
- `.claude/hooks/skill-forced-eval.cjs`
- `.claude/hooks/skill-router.cjs`
- `.claude/hooks/test/skill-router.test.cjs`
- `AGENTS.md`
- `CLAUDE.md`

任何任务开始前执行 `git status -s`；修改既有文件前执行 `git log -1 --format="%ar|%s" <file>`。禁止 stash、reset、checkout 丢改动和全量暂存。

---

## 依赖图

```text
T-01 基线冻结
 ├─ T-02 Claude 运行时探针 ─┐
 └─ T-03 Codex 运行时探针 ──┴─ T-04 P0 结论冻结
                                      ↓
                             T-05 manifest/registry 基础
                                      ↓
                             T-06 路由状态与可信信封
                              ├─ T-07 Claude preflight
                              └─ T-08 Codex preflight
                                  ↓
                    ┌─ T-09 Claude SubagentStart
                    └─ T-10 Codex SubagentStart
                                  ↓
                         T-11 根规则与上下文预算
                                  ↓
                         T-12 中立角色源与 staging 生成
                                  ↓
                         T-13 静态门禁与 staging 验收
                                  ↓
                         T-14 激活与回滚准备
                                  ↓
                         T-15 维护窗口逻辑原子激活
                                  ↓
                         T-16 重启后运行时验收
```

当前执行模式为 `dev-loop`，按 T-01 至 T-16 顺序消费。若以后明确切换为 `plan-executor`，仅允许并行 T-02/T-03、T-07/T-08、T-09/T-10；其余保持串行。

---

## 实现步骤

- [ ] T-01. 冻结现有技能与 agent 治理基线（类型：配置/验证）
  - **文件**：`.agent-governance/baseline/collect-baseline.cjs`、`.agent-governance/baseline/subagent-governance-baseline.json`、`.agent-governance/fixtures/router-fixtures.json`、`.agent-governance/reports/run-regression.cjs`
  - **框架约束**：只增加统计和脱敏字段，不改变现有路由结果；基线不得保存任务原文、绝对用户目录或敏感环境变量。
  - **步骤**：记录四个旧 agent 的名称、路径、工具/权限和正文体积；记录根规则委派段、Hook 配置和输出体积；执行现有路由/安全测试；写入独立 subagent 基线 JSON，避免覆盖历史上下文优化基线。
  - **验证**：运行 `node .agent-governance/scripts/verify-agent-assets.cjs`、`node .claude/hooks/test/pre-tool-use.test.cjs`、`node .claude/hooks/test/skill-router.test.cjs`、`node .codex/hooks/test/skill-router.test.cjs`，预期均退出 0。
  - **依赖**：无。
  - **提交**：`test(agent-governance): 冻结子代理治理基线`

- [ ] T-02. 采集 Claude Code 子代理真实 Hook 结构（类型：技术探针）
  - **文件**：`.claude/hooks/test/runtime-hook-probe.cjs`、`.agent-governance/fixtures/subagent-hook-input-fixtures.json`
  - **框架约束**：只保存字段名、类型、是否存在、固定枚举和内容 hash；禁止保存 `prompt`、transcript 正文或用户输入。临时 Hook 注册必须在本任务结束前撤销。
  - **步骤**：为 `Agent` 和 `SubagentStart` 临时注册脱敏探针；分别从主线程创建只读 agent、从一级 agent 尝试再次创建 agent；记录 `tool_name/tool_input/agent_id/agent_type` 的结构；撤销临时注册并验证原 Hook 配置恢复。
  - **验证**：人工检查 fixture 不含任务正文；运行 `node .claude/hooks/test/pre-tool-use.test.cjs` 和 `node .agent-governance/scripts/verify-agent-assets.cjs`，预期退出 0。
  - **依赖**：T-01。
  - **提交**：`test(claude-hooks): 采集子代理脱敏运行时结构`

- [ ] T-03. 采集 Codex 子代理真实 Hook 结构与递归可观测性（类型：技术探针）
  - **文件**：`.codex/hooks/test/runtime-hook-probe.cjs`、`.agent-governance/fixtures/subagent-hook-input-fixtures.json`
  - **框架约束**：仅追加 Codex 脱敏事件；项目 trust 与 Hook 定义审阅是本机运行态，不写入仓库秘密；不得解析不稳定 transcript 正文。
  - **步骤**：临时注册 `Agent/spawn_agent` 与 `SubagentStart` 探针；采集主线程 spawn、一级子代理再次 spawn 的字段结构；验证 `turn_id/agent_id/session_id` 是否能稳定关联调用者与深度；撤销临时注册并恢复现有 hooks.json。
  - **验证**：人工检查 fixture 不含任务正文；运行 `node .codex/hooks/test/skill-router.test.cjs`，预期退出 0；在 `/hooks` 确认临时定义已移除。
  - **依赖**：T-01。
  - **提交**：`test(codex-hooks): 采集子代理脱敏运行时结构`

- [ ] T-04. 冻结 P0 能力结论与失败边界（类型：系统文档/配置）
  - **文件**：`.agent-governance/reports/subagent-runtime-probe.json`、`.agent-governance/fixtures/subagent-hook-input-fixtures.json`
  - **框架约束**：结论只能来自 T-02/T-03 真实事件；Codex 深度能力枚举为 `hard-capable`、`soft-only`、`unobservable`，无证据时不得使用 `hard-capable`。
  - **步骤**：汇总四类 Hook 字段；冻结 Claude 严格任务字段与 agent_id 规则；冻结 Codex 严格任务字段与身份关联结论；记录 Hook timeout、工作目录、Windows 命令和启动上下文的真实边界。
  - **验证**：逐项比对 probe JSON 与脱敏 fixture；运行一个 schema 校验脚本，预期无缺失结论、无 prompt/transcript 正文。
  - **依赖**：T-02、T-03。
  - **提交**：`docs(agent-governance): 固化双端子代理能力结论`

- [ ] T-05. 建立最小 agent manifest 与 registry（类型：配置/非CRUD骨架）
  - **文件**：`.agent-governance/agents-manifest.json`、`.agent-governance/lib/agent-registry.cjs`、`.agent-governance/fixtures/agent-registry-fixtures.json`、`.agent-governance/test/agent-registry.test.cjs`
  - **框架约束**：技能触发词仍只来自 `skills-manifest.json`；agent manifest 只管角色、风险、能力 ID 和 runtime 资源。`check` 必须映射为 Claude Command 与 Codex Skill，不得硬编码进适配器。
  - **步骤**：定义 schemaVersion、保留名、深度策略和 runtime 名；登记内置角色风险级别；登记三个 `ruoyi-*` 项目角色；实现能力 `kind/path/preloadable`、存在性、保留名和风险解析。
  - **验证**：运行 `node .agent-governance/test/agent-registry.test.cjs`，覆盖合法角色、保留名冲突、缺失资源、Claude Command 误预载、未知角色和三个风险等级。
  - **依赖**：T-04。
  - **提交**：`feat(agent-governance): 建立子代理注册表`

- [ ] T-06. 实现可诊断路由、可信信封与公共 preflight（类型：非CRUD骨架）
  - **文件**：`.agent-governance/lib/router.cjs`、`.agent-governance/lib/delegation-envelope.cjs`、`.agent-governance/lib/agent-preflight.cjs`、`.agent-governance/fixtures/agent-preflight-fixtures.json`、`.agent-governance/test/agent-preflight.test.cjs`
  - **框架约束**：保留 `selectRoute()` 旧返回形状和消费者；新增 `selectRouteWithStatus()` 区分四类状态。只替换消息末尾完整生成块，每次重算；动态 required 不静默丢弃，基线能力优先于 optional。
  - **步骤**：增加路由诊断状态；实现末尾信封解析/移除/渲染；实现严格 runtime extractor、风险分级、字段保留和 deny/degrade；增加仅记录状态/agent/config hash 的审计事件。
  - **验证**：运行 `node .agent-governance/test/agent-preflight.test.cjs`、现有双端 skill-router 测试和 `run-regression.cjs`，预期全部通过。fixture 必须覆盖伪造/截断标记、正常 no-match、manifest 错误、字段缺失、required 超限、baseline 裁剪和隐私检查。
  - **依赖**：T-05。
  - **提交**：`feat(agent-governance): 实现可信技能路由信封`

- [ ] T-07. 接入 Claude Agent preflight 与递归门禁（类型：配置/非CRUD骨架）
  - **文件**：`.claude/hooks/agent-skill-preflight.cjs`、`.claude/hooks/pre-tool-use.cjs`、`.claude/hooks/test/agent-skill-preflight.test.cjs`、`.claude/hooks/test/pre-tool-use.test.cjs`、`.claude/settings.json`
  - **框架约束**：使用 `command: node` + `args: ["${CLAUDE_PROJECT_DIR}/..."]` 的跨平台 exec form；timeout 按 P0 实测单位配置；数组合并现有 Hook。若 matcher 增加 PowerShell，安全脚本和测试必须同步覆盖。
  - **步骤**：实现 Claude `tool_input.prompt` 适配；输入存在 `agent_id` 且再次调用 Agent 时 deny；对高风险错误 deny、只读错误降级；修正 settings 中所有相关 Node Hook 的 Windows 路径与合理 timeout；保留已有字段。
  - **验证**：运行 `node .claude/hooks/test/agent-skill-preflight.test.cjs`、`node .claude/hooks/test/pre-tool-use.test.cjs`、`node .claude/hooks/test/skill-router.test.cjs`，并用真实一级子代理验证递归 deny。
  - **依赖**：T-06。
  - **提交**：`feat(claude-hooks): 接入子任务技能预检`

- [ ] T-08. 接入 Codex Agent preflight 与深度策略（类型：配置/非CRUD骨架）
  - **文件**：`.codex/hooks/agent-skill-preflight.cjs`、`.codex/hooks/test/agent-skill-preflight.test.cjs`、`.codex/hooks.json`、`.codex/config.toml`
  - **框架约束**：严格读取 `spawn_agent.message`；相对路径从 Git 根解析，并提供可处理空格路径的 `commandWindows`；合并而非覆盖 SessionStart、UserPromptSubmit、安全 PreToolUse 和 Stop。
  - **步骤**：实现 Codex function-tool 适配和完整 `updatedInput`；根据 T-04 选择 hard deny 或 soft-only 审计，不自行推断 agent 身份；补 `[agents]` 并发与 reasoning 配置；检查运行时是否存在第二个 Agent updatedInput 重写器。
  - **验证**：运行 `node .codex/hooks/test/agent-skill-preflight.test.cjs`、`node .codex/hooks/test/skill-router.test.cjs`；分别从仓库根目录和一级子目录触发 Hook；在 `/hooks` 审阅新定义。
  - **依赖**：T-06。
  - **提交**：`feat(codex-hooks): 接入子任务技能预检`

- [ ] T-09. 实现 Claude SubagentStart 启动上下文（类型：配置/非CRUD骨架）
  - **文件**：`.agent-governance/lib/subagent-context.cjs`、`.claude/hooks/subagent-start.cjs`、`.claude/hooks/test/subagent-start.test.cjs`、`.claude/settings.json`
  - **框架约束**：matcher 覆盖全部 agent；启动 Hook 只注入角色边界、信封检查和未知角色状态，不能承担动态路由或阻止启动；输出不得含任务正文。
  - **步骤**：实现公共启动上下文渲染器；Claude 适配 agent_type/agent_id；已登记角色注入专属边界，未知角色注入 `UNREGISTERED_AGENT`；加入 token/字节预算统计。
  - **验证**：运行 `node .claude/hooks/test/subagent-start.test.cjs`，覆盖内置、项目、未知角色、超预算和无 manifest；真实启动 Explore 与项目 reviewer 检查首条上下文。
  - **依赖**：T-07、T-08。
  - **提交**：`feat(claude-hooks): 注入子代理启动边界`

- [ ] T-10. 实现 Codex SubagentStart 启动上下文（类型：配置/非CRUD骨架）
  - **文件**：`.codex/hooks/subagent-start.cjs`、`.codex/hooks/test/subagent-start.test.cjs`、`.codex/hooks.json`
  - **框架约束**：复用 T-09 公共实现；`continue:false` 不能被当作阻断；不解析 transcript；命令从 Git 根解析并包含 `commandWindows`。
  - **步骤**：适配 Codex agent_type/agent_id/turn_id；注册全 agent matcher；注入已登记/未知角色上下文；按 T-04 结论加入深度状态或 soft-only 提示。
  - **验证**：运行 `node .codex/hooks/test/subagent-start.test.cjs`；在 `/hooks` 确认定义已信任；真实启动 explorer 和未知自定义角色验证上下文。
  - **依赖**：T-07、T-08。
  - **提交**：`feat(codex-hooks): 注入子代理启动边界`

- [ ] T-11. 收敛全局委派规则、dev-loop contract 与上下文预算（类型：系统文档/配置）
  - **文件**：`.agent-governance/core-rules.md`、`.claude/commands/dev-loop.md`、`AGENTS.md`、`CLAUDE.md`、`.agent-governance/baseline/subagent-governance-baseline.json`
  - **框架约束**：先合并当前未提交的 core/root 变更；只改源规则并运行同步脚本，不手改生成物。根规则只保留短硬原则，详细矩阵留在方案和任务卡。
  - **步骤**：替换过宽的“跨多文件即委派”规则；加入不委派条件、写所有权、父代理责任和运行时降级；把 dev-loop 派单改为目标/范围/模式/所有权/约束/验收/回传 contract；冻结五类上下文预算及测量方式。
  - **验证**：运行 `node .agent-governance/scripts/sync-agent-assets.cjs`、`node .agent-governance/scripts/verify-agent-assets.cjs`；比较 AGENTS/CLAUDE 生成结果与预算，预期一致且无手工漂移。
  - **依赖**：T-09、T-10。
  - **提交**：`refactor(agent-governance): 收敛子代理委派规则`

- [ ] T-12. 建立中立角色正文和 staging 生成器（类型：配置/系统文档）
  - **文件**：`.agent-governance/agents/ruoyi-code-reviewer.md`、`.agent-governance/agents/ruoyi-project-reporter.md`、`.agent-governance/agents/ruoyi-project-updater.md`、`.agent-governance/agents-manifest.json`、`.agent-governance/scripts/sync-agent-assets.cjs`、`.agent-governance/scripts/test/sync-agent-assets.test.cjs`
  - **框架约束**：角色正文不复制技能全文；reporter 无写能力，updater 只在用户明确授权时写 `docs/`；生成器先输出 `.agent-governance/staging/{claude,codex}/agents/`，不得提前写运行时发现目录。
  - **步骤**：编写三个轻量中立正文；补全 runtime tools/mode/model/能力和预算；实现 Claude YAML、Codex TOML 渲染；实现 staging、`--check`、UTF-8/LF 和可重复生成。
  - **验证**：运行 `node .agent-governance/scripts/test/sync-agent-assets.test.cjs` 和 `node .agent-governance/scripts/sync-agent-assets.cjs --check --staging`；两次生成 hash 必须一致。
  - **依赖**：T-11。
  - **提交**：`feat(agent-governance): 单源生成项目子代理`

- [ ] T-13. 扩展静态门禁并验收 staging（类型：验证）
  - **文件**：`.agent-governance/scripts/verify-agent-assets.cjs`、`.agent-governance/fixtures/agent-preflight-fixtures.json`、`.agent-governance/staging/claude/agents/`、`.agent-governance/staging/codex/agents/`
  - **框架约束**：校验器必须早于激活；不得通过跳过目录、转警告或放宽断言获得通过。staging 为临时生成物，是否提交由生成策略明确，不能成为第三份手工源。
  - **步骤**：增加 manifest schema/保留名/runtime 资源/工具能力检查；解析 staging YAML/TOML；检查 BOM/LF/字面 `\r`/跨端路径/预算；检查 Hook 接线、旧 API 兼容和生成物漂移。
  - **验证**：运行 `node .agent-governance/scripts/verify-agent-assets.cjs` 与全部新增单测；主动构造一个可恢复的 staging 漂移，确认校验非 0 后恢复并重跑通过。
  - **依赖**：T-12。
  - **提交**：`test(agent-governance): 增加项目子代理静态门禁`

- [ ] T-14. 准备激活清单、回滚材料与维护窗口（类型：配置迁移）
  - **文件**：`.agent-governance/reports/subagent-activation-manifest.json`、`docs/brainstorm-子代理技能匹配统一治理.md`、本任务卡
  - **框架约束**：此任务只准备，不删除旧 agent；Windows 多文件切换称“维护窗口内逻辑原子”，不宣称文件系统物理原子。禁止使用 stash/reset/checkout 丢改动作为回滚。
  - **步骤**：冻结新生成物 hash 和旧角色精确路径；全仓列出旧名引用；记录上一版治理源/生成器提交或可恢复内容；列出切换顺序、失败条件、重启要求和用户维护窗口确认项。
  - **验证**：逐项核对 activation manifest 中的新增、替换、删除和回滚目标均为精确路径；确认 staging 全绿且运行时发现目录尚未出现 `ruoyi-*`。
  - **依赖**：T-13。
  - **提交**：`docs(agent-governance): 准备子代理激活与回滚清单`

- [ ] T-15. 在维护窗口执行新旧角色逻辑原子切换（类型：配置迁移）
  - **文件**：`.claude/agents/ruoyi-*.md`、`.codex/agents/ruoyi-*.toml`、旧 `.claude/agents/{code-reviewer,project-manager}.md`、旧 `.codex/agents/{code-reviewer,project-manager}.toml`、`docs/AI体系学习笔记.md`、`docs/AI助手实战使用指南.md`
  - **框架约束**：必须先确认无其他活动 Claude/Codex 会话和同路径他者改动；新文件由生成器写入，旧文件在同一受控变更集删除；不手改生成物。删除旧 agent 属于已批准方案范围，但执行时仍需再次核对精确路径。
  - **步骤**：从 staging 写入三个 Claude 与三个 Codex 新角色；切换所有当前操作文档和流程引用；删除四个旧角色文件；运行静态门禁；停止当前运行时并为 T-16 重启验收保留状态。
  - **验证**：`Get-ChildItem .claude/agents,.codex/agents` 只出现新项目角色；`git grep` 无未说明的旧名；`node .agent-governance/scripts/verify-agent-assets.cjs` 退出 0。失败时按 T-14 从上一版治理源重新生成回滚。
  - **依赖**：T-14、用户确认维护窗口。
  - **提交**：`refactor(agent-governance): 激活双端项目子代理`

- [ ] T-16. 重启后完成双端运行时验收与任务收口（类型：验证）
  - **文件**：`.agent-governance/reports/subagent-runtime-acceptance.json`、`docs/brainstorm-子代理技能匹配统一治理.md`、本任务卡
  - **框架约束**：静态测试不能替代运行时验收；报告只保存角色、状态、路径、大小、hash 和结论，不保存任务正文。失败时回滚整个激活变更集，不现场手修生成物。
  - **步骤**：重启 Claude/Codex；Claude 验收 Explore、reviewer、reporter、updater 的发现、首条上下文、技能读取和递归 deny；Codex 验收 trust、`/hooks`、explorer/worker/三个项目角色和深度策略；从根目录、一级子目录、含空格路径各验证一次；记录上下文预算和误派情况。
  - **验证**：运行下方计划级验证命令，预期全部退出 0；人工验收清单全部通过；更新方案状态与任务进度。失败立即执行 T-14 回滚并重启复核。
  - **依赖**：T-15、双端运行时已重启。
  - **提交**：`test(agent-governance): 验收双端子代理技能治理`

---

## 计划级验证命令

以下命令按对应任务创建测试文件后逐步启用；P0 不得把尚不存在的命令伪装成已有基线。

```powershell
node .agent-governance/scripts/verify-agent-assets.cjs
node .claude/hooks/test/pre-tool-use.test.cjs
node .claude/hooks/test/skill-router.test.cjs
node .codex/hooks/test/skill-router.test.cjs
node .agent-governance/test/agent-registry.test.cjs
node .agent-governance/test/agent-preflight.test.cjs
node .claude/hooks/test/agent-skill-preflight.test.cjs
node .codex/hooks/test/agent-skill-preflight.test.cjs
node .claude/hooks/test/subagent-start.test.cjs
node .codex/hooks/test/subagent-start.test.cjs
node .agent-governance/scripts/test/sync-agent-assets.test.cjs
node .agent-governance/scripts/sync-agent-assets.cjs --check
node .agent-governance/reports/run-regression.cjs
git diff --check
```

## 运行时人工验收清单

- [ ] Claude：`Explore`、`ruoyi-code-reviewer`、`ruoyi-project-reporter`、`ruoyi-project-updater` 均可发现。
- [ ] Claude：项目角色读取正确的 Skill/Command 路径，一级子代理再次委派被真实拒绝。
- [ ] Codex：项目 `.codex/` 已 trusted，新增/变化 Hook 已在 `/hooks` 审阅。
- [ ] Codex：`explorer`、`worker` 与三个 `ruoyi-*` 角色均可发现，信封与 P0 深度策略一致。
- [ ] 两端：根目录、一级子目录、含空格克隆路径的 Hook 均可定位脚本。
- [ ] 两端：只读错误可降级，高风险错误真实 deny；伪造标记不能绕过。
- [ ] 两端：主线程只接收短摘要，不接收原始日志或大段文件内容。
- [ ] 激活失败回滚后旧角色完整恢复、新角色完整移除，重启后发现状态一致。

---

## 关键决策

- **执行顺序修正**：最小 `agents-manifest`/registry 前移到 preflight 前，因为风险分级与 `SubagentStart` 都依赖角色元数据；P3 只负责补全中立正文与生成器。
- **失败策略**：`no-match` 是正常结果；`router-error`/`invalid-input` 对只读探索 fail-open，对 review/write/docs-write fail-closed。
- **深度策略**：Claude 使用 `agent_id` 硬门禁；Codex 以 P0 真实探针为唯一裁决依据。
- **资源模型**：agent 引用能力 ID；每个 runtime 显式声明 `kind/path/preloadable`。
- **角色拆分**：项目状态查询与项目文档更新分别使用 reporter/updater，避免单角色静态写权限泄漏。
- **迁移策略**：先 staging、后 verifier、再维护窗口逻辑原子切换；运行时发现目录不承载手工双源。
- **兼容策略**：保留现有 `selectRoute()` 外部行为和 fixture，新增诊断 API，不重写已有消费者。

## 问题与阻塞记录

| 编号 | 问题 | 当前处理 |
|---|---|---|
| B-01 | 仓库不存在 `docs/需求文档.md` | 使用已确认 brainstorm 文档作为上位需求，计划中显式记录 |
| B-02 | Codex 子代理调用者/深度是否可稳定关联尚未证实 | T-03/T-04 技术探针；未证实即 soft-only |
| B-03 | 多配置层可能存在多个 Agent `updatedInput` 重写器 | 静态告警 + `/hooks` 人工检查；不把 Hook 当完整安全边界 |
| B-04 | core-rules、根规则和部分 skill Hook 已有未提交改动 | 每任务先审 diff，合并而非覆盖 |
| B-05 | 当前 agent-context-optimization 任务卡虽已完成但仍位于 active | 仅作为已完成基线引用，不重复其 T-01～T-09，不自动归档 |

## 当前进度

**已完成**：0 / 16 个实施任务（0%）

**正在进行**：计划已生成，等待从 T-01 开始执行。

**下一步**：执行 T-01，冻结现有路由、agent、Hook 和上下文体积基线。

**阻塞问题**：无；T-15 需要用户在执行时确认维护窗口。

## 相关文件

- `docs/brainstorm-子代理技能匹配统一治理.md`：上位设计与最终决策。
- `docs/tasks/active/task-20260819-101448-agent-context-optimization.md`：已完成的技能路由/上下文治理基线。
- `.agent-governance/skills-manifest.json`：技能触发词唯一来源。
- `.agent-governance/lib/router.cjs`：现有公共技能路由器。
- `.agent-governance/scripts/sync-agent-assets.cjs`：现有根规则生成器，后续扩展 agent 生成。
- `.agent-governance/scripts/verify-agent-assets.cjs`：现有治理门禁，后续扩展 agent 校验。

## 变更记录

### 2026-08-20 16:53:24

**变更类型**：创建执行计划

**变更内容**：

- 依据已确认 brainstorm 方案生成 16 个可执行任务。
- 执行模式判定为 `dev-loop`。
- 将 agent manifest/registry 前移，消除风险分级的依赖倒置。
- 将真实运行时探针、staging、静态门禁、维护窗口和回滚拆成独立任务。
